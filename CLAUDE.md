# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## О проекте

Сайт записи на тур в Териберку: посетитель заполняет форму на главной и ждёт звонка. Полноценной авторизации нет и не планируется.

Просмотр поступивших заявок на сайте намеренно **не делается** — заявки будут читаться из Telegram-бота (решение от 11.08.2026). Бот живёт **внутри chat-сервиса** ([chat/service/Bot/](src/backend/chat/service/Bot/)) — переехал туда из users 21.08.2026 вместе с чатом (см. «Telegram-бот»): в личке отвечает приветствием с кнопкой-ссылкой на сайт, в группе гидов обслуживает чат. Чтение заявок в него ещё не добавлено, серверная часть готова: `GET /api/admin/application/list` и репозиторий, сортирующий заявки свежими сверху. **Важно**: после переезда бот уже не может читать заявки через репозитории напрямую — ходить придётся в users по HTTP через шлюз с постоянным JWT.

Бренд сайта — «ТериберкаКрай» (en: TeriberkaKray, zh: 捷里别尔卡之境; переименован владельцем 24.08.2026 под домен teriberka-kray.ru, раньше был «Кольский Север»): туры не только в Териберку, а по трём направлениям Мурманской области. Состав: три .NET 10 сервиса (`gateway` на YARP + `users` с БД + `chat` с БД и Telegram-ботом) и Blazor-фронтенд на static SSR: лендинг (hero: тексты и факты слева + интерактивная карта справа (фото-карусель в hero пробовали 24.08.2026 — тесни́ла карту, фото уехали в полосу ниже, которой сейчас в разметке нет — см. дальше), под ними — «Что вы увидите»: карточки мест Териберки с **собственными фото владельца** (5 из 6 заменены 26.08.2026, см. «Фото мест»; `.hero-places`, id=places; на ≤560px — карусель по одной карточке во всю ширину со стрелками carousel.js, обёртка `.places-carousel`; раньше лента показывала край соседней карточки — владелец счёл это обрезом, 24.08.2026; иначе шесть карточек занимали ~6 экранов), ссылки в «Галерею» в блоке сейчас нет: примечание `.places-more` («(Смотрите больше фото и видео в галерее)») стояло в шапке блока под подводкой и **временно убрано** владельцем 25.08.2026 — раздел `/gallery` пока заглушка «скоро», вести туда не с чего. Ключ `PlacesGalleryLink`, стили `.places-more` и сама строка разметки (в комментарии рядом) сохранены; скобки — в разметке, не в ключе, пунктуация одна на все языки. **Кнопок в hero нет** (владелец, 25.08.2026: сначала посетитель смотрит фото и читает программу, кнопки — после неё): до программы дня заявку держит только шапка — `.nav-apply` на десктопе и `.header-apply` на мобиле. На десктопе grid-area ставит текст и карту в две колонки, карточки мест — во всю ширину под ними. Полоса «Что вас ждёт» (`#highlights`) **из разметки главной пропала** в коммите 969626f — её место в hero занял блок мест, а `HeroSlides` вместе с `MediaCatalog` и всем S3-конвейером остались без единого потребителя; решение о возврате отложено владельцем 25.08.2026. Что о ней известно, если возвращать: полоса — карусель (3:2 на телефоне, 21:9 от 700px; список слайдов — [HeroSlides](src/frontend/ui.public.web/Features/Home/HeroSlides.cs): имена файлов `wwwroot/img/hero-*.webp` и ключи названия/alt `HeroSlide{n}Title/Alt`). **Фото — собственные снимки владельца** (кадры с Wikimedia Commons удалены 24.08.2026): тринадцать слайдов в порядке «зима → обед из моря → лето» (владелец, 24.08.2026): зима — `hero-winter-sun`, `hero-winter-ship` (корабль с граффити), `hero-winter-bay`, `hero-winter-surf`, `hero-winter-blizzard`, `hero-wind`; еда — `hero-seafood` (ежи, гребешки, звезда), `hero-urchin`, `hero-shell`; лето — `hero-edge` («Край Земли»), `hero-bay`, `hero-ships`, `hero-gorge`. Кроп 5:4 1500×1200 из оригиналов в `src/frontend/temp` (IMG_3813/3800 и IMG_20260207_134359/112319/121222/120201 JPG, IMG_6669/6662/6665/6180/6390/6102/6554 HEIC; вертикальные пейзажи кропались с фокусом, чтобы сюжет попал в центральную полосу 21:9; три «съедобных» кадра помечены `Portrait` в HeroSlides — файлы 4:5 1200×1500, в ленте показываются целиком (`object-fit: contain`) поверх своей же размытой копии `.photo-slide-bg`, иначе от 9:16-оригинала оставалась узкая середина; «Драконьи яйца» IMG_6564 были и убраны владельцем); пока файла нет, Home.razor подставляет `placeholder.svg` (проверка `File.Exists` в webroot), чтобы не было битых картинок. Видимых подписей на кадрах **нет** (были `figcaption`, убраны владельцем 24.08.2026 — текст спорил с сюжетом; остались только alt по ключам `HeroSlide{n}Alt`), подводка секции сознательно абстрактная — не перечисляет сюжеты, чтобы не расходиться с набором фото; тонировка `.photo-strip::after` уводит любой кадр в палитру ночи, автопрокрут — см. «Фото мест»; в src картинок `?v=4` — файлы под теми же именами менялись, а кеш картинок 30 дней) → программа дня в Териберке — **вертикальный маршрут** `.itinerary-route` (владелец, 25.08.2026, вместо сетки текстовых карточек): пунктирная нить как у маршрутов на карте, поверх неё аврора-линия прогресса, которая «рисуется» прокруткой (route-progress.js, см. список скриптов), на нити точки-номера — загораются, когда линия до них доходит, — у каждой из 6 остановок карточка с фото и описанием; на мобиле нить слева, от 900px — по центру, карточки шахматкой с перехлёстом `margin-top: -120px` (стороны строго чередуются, поэтому перехлёст безопасен). Фото — реальные кадры владельца в `wwwroot/img/itinerary/step-*.webp` (25.08.2026, из `src/frontend/temp` через convert-photos.py на 1000px; кадр 16/10 — половина фото вертикальные, шире резало бы их до полосы), список у остановок — массив `RoutePhotos` в Home.razor: несколько имён = карусель carousel.js внутри карточки (`.route-photo-carousel`, свайп без JS, стрелки прижаты внутрь — у карточки overflow hidden; `data-autoplay` разведён по остановкам — 5.6/6.3/7.0/7.7 с, чтобы карусели не листались синхронно). Кадры подобраны «по красоте» и могут не совпадать с шагом дословно — владелец заменит по мере съёмки: файл в каталог + имя в RoutePhotos (миниатюры мест у шагов убирались 24.08.2026 — то были мелкие ссылки, спорившие с текстом; тут фото — часть карточки, без ссылок; шаг 6 — «Возвращение в город», без привязки к Мурманску: выезд бывает и из Апатитов/Кировска), сразу под ней — блок `.tour-cta`: кнопка «Оставить заявку» (якорь `#apply` на форму внизу) и строка цены с форматом тура. Бейджи «Без предоплаты» убраны владельцем 25.08.2026 — и строка `.hero-trust` («Заявка без предоплаты и обязательств», ключ `HeroTrust`), и кикер `FormKicker` над формой; ключи и стили целы, само обещание осталось прозой в подводке формы и в «почему с нами», а у ветки Custom кикер свой («Индивидуальный тур») и остался. Блок переехал сюда из hero 25.08.2026; в тот же день его пробовали заменить самой формой, но владелец вернул кнопку. «Призрачной» `.btn-ghost` к программе дня рядом нет — она вела бы в блок прямо над собой (ключ `HeroCta2` в resx остался). **Форма на странице ровно одна** — у static SSR `FormName` уникален, второй `ApplicationForm` на той же странице упадёт; поэтому «показать форму и вверху, и внизу» не вариант. Дальше: «что включено» (сразу за строкой цены — это состав оффера, ответ на «что я получу за эти деньги», а не работа с сомнениями; владелец, 25.08.2026) → параллакс-полоса (закрывает териберскую часть: текст про Баренцево море — её финальный аккорд; между «почему с нами» и FAQ она резала пополам блок работы с сомнениями, владелец 25.08.2026) → другие маршруты Кольского (не записался на Териберку — посмотри остальные направления) → форма (только имя — необязательно — и телефон; дата, состав группы и пожелания сознательно убраны 21.08.2026 — уточняются по телефону; рядом с кнопкой отправки — `.btn-tg`, ссылка на Telegram-бота при заданном `TG_BOT_URL`) → и уже для сомневающихся «почему с нами» — 4 пункта (четвёртый, `Why4*` — «за рулём гид, а не случайный водитель»: опыт дороги в любое время суток и года, владелец 26.08.2026), сознательно без дублей с «что включено»; на десктопе сетка 2×2, а не три колонки — при четырёх пунктах справа оставалась дыра → FAQ на `<details>` (6 вопросов, последний — «что происходит после заявки»). Блок «Нужен индивидуальный тур?» в конце страницы **убран** владельцем 25.08.2026: индивидуальный маршрут стал одним из вариантов в самой форме, и отдельный призыв его дублировал; ключи `CustomTour*` сохранены, ссылок на ветку `?tour=custom` со страницы больше нет. Его место занял финальный призыв `#final-cta` (ключи `FinalCtaTitle`/`FinalCtaText`, кнопка по `HeroCta`, оформление тех же `.custom-tour`, кикера нет). Над кнопкой — фраза `.final-cta-lead` с тремя путями к нам: форма, Telegram-бот и чат («спросите нас **прямо отсюда**»). Собирается из кусков `FinalCtaLead`/`FinalCtaBotPrefix`/`FinalCtaBot`/`FinalCtaOr`/`FinalCtaHere`, а не из одного ключа с разметкой: внутри две ссылки, а порядок слов в трёх языках разный (тот же приём, что у согласия в форме). Ссылка на бота рендерится только при заданном `TG_BOT_URL`; «прямо отсюда» несёт класс `contact-chat` — тот же, что у пункта виджета связи, поэтому chat.js открывает панель на месте, а без JavaScript страница перезагрузится с `?chat=open`. Отдельной подсказки «кнопка в правом нижнем углу» нет — владелец счёл её разжёвыванием, 25.08.2026): за FAQ сразу начиналась морская зона с футером, и дочитавшему хвост было некуда нажать. Якорь `#apply` из него скроллит вверх — форма на странице одна, это осознанно) плюс страницы мест `/place/{slug}` (13 мест). Блок «Териберка в разное время года» убран владельцем 25.08.2026 вместе со второй параллакс-полосой (`band-2`, `Band2Text`): без сезонов две полосы шли бы подряд; ключи `Season*`/`SeasonNote`/`Band2Text`, стили `.seasons-grid`/`.season-card` и ссылка `NavSeasons` в resx остались, из футера ссылка на `/#seasons` убрана. Кикер «Не только Териберка» над «Другими маршрутами» тоже снят (ключ `OtherRoutesKicker` цел). Гиды и статистика из hero удалены сознательно — вымышленный контент; не возвращать без настоящих данных. Отзывы возвращены владельцем 28.08.2026 (блок `.reviews` после FAQ, перед финальным призывом — см. «Отзывы» ниже) — тексты пока черновики, заменить на настоящие. Настоящий гид Серёга Северный — исключение (фото [img/guide.webp](src/frontend/ui.public.web/wwwroot/img/guide.webp) и факты предоставлены владельцем 18.08.2026, конкретных цифр в тексте сознательно нет), но блок `.guide-note` у формы **закомментирован 21.08.2026**: его фото теперь работает лицом плавающей кнопки связи (см. «Виджет связи»), и второй раз перед формой не нужно. Разметка, стили и ключи `GuideTitle`/`GuideText` сохранены — блок вернётся по желанию владельца. `<meta name="description">` локализован (`MetaDescription`). Часть комментариев — на русском.

**Контент-плейсхолдеры, которые надо заменить перед запуском**: ссылка `TG_BOT_URL` (username бота пока выдуманный, и на бою ссылка **ведёт в никуда** — владелец решил 28.08.2026 оставить её видимой до создания бота; чтобы спрятать все пять мест, где сайт на него ссылается, нужен явный `TG_BOT_URL: ""` в [docker-compose.prod.yml](docker-compose.prod.yml): пустая строка в `.env` не поможет, базовый compose подставляет дефолт через `${TG_BOT_URL:-…}`, а такой синтаксис перекрывает и пустое значение) и текст политики конфиденциальности. Телефон, почта и профиль в MAX настоящие (владелец, 26–28.08.2026): `CONTACT_PHONE` = +7 911 303-13-43, `CONTACT_EMAIL` = teriberka-kray@yandex.ru (футер и `/privacy`; в виджете связи пункта почты нет), `MAX_URL` = `https://max.ru/u/…` (длинный токен профиля, не username — не «поправлять» руками). Футер берёт контакты из тех же конфиг-ключей, что и виджет связи: пустой ключ прячет строку, а не даёт битую ссылку. WhatsApp убран владельцем 26.08.2026, вместо него MAX. Реквизиты (`FooterLegal`, общий ключ футера и `/privacy`) уже **настоящие** — «Диденко Сергей Денисович · ИНН 511100738876» (владелец, 26.08.2026); ОГРНИП не указан, потому что владелец его не сообщал: если он ИП, строку нужно дополнить. Текст политики ([PrivacyView.razor](src/frontend/ui.public.web/Features/Legal/Pages/PrivacyView.razor) — черновик, о чём честно говорит `PrivacyDraftNote`).

## Команды

```powershell
dotnet build Teriberka.sln                # сборка решения (~20 c, 0 ошибок; ~150 warning'ов — норма)
docker compose up -d postgres             # только БД для локальной разработки
docker compose up --build                 # весь стек: БД + gateway + users + chat + UI + minio + nginx
docker compose -f docker-compose.yml -f docker-compose.prod.yml up -d --build   # боевой сервер, см. docs/deploy.md
docker compose up -d minio minio-init     # только хранилище фото (консоль на http://localhost:9001)
```

В контейнерах сайт открывается на **http://localhost** (порт 80 через nginx), а не на :7002 — порт `ui-public` наружу больше не публикуется. Перед первым запуском скопировать `.env.example` в `.env` и заполнить пароли: без `MINIO_ROOT_PASSWORD` и `MEDIA_SECRET_KEY` compose откажется стартовать. Это намеренно — иначе хранилище поднялось бы с дефолтными креденшелами.

Фото для сайта: оригиналы владельца (HEIC/JPG с телефона) лежат в `src/frontend/temp/` (в .gitignore), конвертирует [tools/convert-photos.py](tools/convert-photos.py) — `python tools/convert-photos.py src/frontend/temp src/frontend/temp/webp --width 1600` (нужны `pip install pillow pillow-heif`; `--aspect 5:4` — кроп под слайды полосы «Что вас ждёт», уже готовые файлы пропускает). Готовые кадры заливаются в хранилище (см. «Фото главной»), локальные копии в `wwwroot/img/` остаются фоллбеком.

Локальный запуск сервисов — **обязательно с явным окружением**: `dotnet run` печатает «Using launch settings…», но переменные из `environmentVariables` профиля не применяет, поэтому окружение остаётся Production и `appsettings.Development.json` не читается. Симптомы: у users и chat — `The ConnectionString property has not been initialized`, у шлюза — 500 на любом запросе (`JWT_KEY` = null).

```powershell
dotnet run --project src/backend/users/service/Users.csproj -- --environment Development        # :5012
dotnet run --project src/backend/chat/service/Chat.csproj -- --environment Development          # :5013
dotnet run --project src/backend/gateway/Gateway.csproj -- --environment Development            # :5000
dotnet run --project src/frontend/ui.public.web/UI.Public.Web.csproj -- --environment Development  # :7002
```

Каталог запуска не важен — ContentRoot всегда выставляется в каталог проекта. Альтернатива флагу — задать `ASPNETCORE_ENVIRONMENT=Development` в окружении или передать нужные ключи (`DB_*_CONNECTION_STRING`, `JWT_KEY`, …) переменными среды.

Миграции — у каждого сервиса свой контекст и своя папка `Migrations`; контекст указывать обязательно:

```powershell
dotnet ef migrations add <Name> `
  --project src/backend/users/modules/Users.Infrastructure `
  --startup-project src/backend/users/service `
  --context WriteApplicationDbContext

dotnet ef migrations add <Name> `
  --project src/backend/chat/modules/Chat.Infrastructure `
  --startup-project src/backend/chat/service `
  --context WriteChatDbContext
```

`database update` не нужен: `EnsureDataBaseAsync` в `Program.cs` применяет миграции при старте сервиса (при окружении `Testing` — `EnsureCreated`).

Тестов нет: тестовых проектов не создано, версии тестовых пакетов из `Directory.Packages.props` удалены. При добавлении тестов сначала верните нужные `PackageVersion` — включено central package management, поэтому в `.csproj` версии не указываются.

### Полезное для отладки

- Swagger-агрегатор шлюза: `http://localhost:5000/swagger` (только Development).
- Заявки посмотреть вручную: `GET http://localhost:5000/dev/token` (только Development) отдаёт JWT с ролью `SuperAdmin`, с ним `GET http://localhost:5000/api/admin/application/list` возвращает список. Тем же путём пойдёт Telegram-бот, только с постоянным токеном.
- Чат проверить без сайта: `POST /api/public/chat/send` с телом `{"text":"…"}` возвращает `value` (токен диалога) и `hash` (Ordinal); дальше `GET /api/public/chat/messages?token=<value>&after=0`. Работает и напрямую в chat (`:5013`), и через шлюз (`:5000`).
- Настроить группу гидов: создать приватную группу → добавить бота → в логе chat появится строка «Бота добавили в чат {ChatId}» → этот id в `TG_ADMIN_CHAT_ID` → перезапуск.
- Health: `/health/ready` и `/health/live` у сервисов, `/health` у шлюза и UI.

## Архитектура

### Путь запроса

```
браузер --POST form--> nginx --> UI (Blazor SSR) --Mediator--> ApiCommandHandler --HTTP--> gateway --> users --> Postgres
браузер --чат--------> nginx --> UI (Blazor SSR) --Mediator--> ApiCommandHandler --HTTP--> gateway --> chat  --> Postgres
                                                                                                       chat  --> Telegram (группа гидов)
браузер --фото-------> nginx --> MinIO
```

Наружу смотрит только nginx; всё остальное — во внутренней сети compose. Фото по `/media/*` **не проходят через Kestrel** вообще: они не тратят rate limit сайта и не участвуют в `UseStaticFiles`.

Заявку сайт создаёт через открытый маршрут `/api/public/application/create`. Чтение заявок наружу отдаётся только через `/api/admin/application/list`, закрытый на шлюзе политикой `Admin`. Чат ходит в отдельный сервис через `/api/public/chat/*` (оба маршрута публичные — см. «Чат с посетителем»).

### UI: тема, статика, локализация

- **JavaScript считается доступным** (владелец, 28.08.2026 — правило «всё работает без JS» снято вместе с добавлением капчи, которая без JS невозможна): новые фичи могут требовать JS без фоллбеков. Всё построенное до этого — тема, язык, формы, карта, карусели, меню на `<details>`, чат через `?chat=open` — работает серверно/декларативно, и **выпиливать эти фоллбеки не нужно**: они уже написаны, ничего не стоят и страхуют от сломавшегося скрипта. Правка остаётся правкой: сломал фоллбек по ходу дела — почини, но строить новый фоллбек ради новой фичи больше не обязательно. CSP — `script-src 'self'` (это правило безопасности, оно НЕ снято; inline-скрипты запрещены); главный скрипт — [wwwroot/js/water.js](src/frontend/ui.public.web/wwwroot/js/water.js): самописный WebGL-шейдер воды (~5KB, без библиотек) в полосе `.shore-band`. Он сам выключается при `prefers-reduced-motion`, без WebGL остаётся CSS-фоллбек из трёх слоёв волн, рендер идёт только когда полоса видна (IntersectionObserver). Остальные скрипты того же образца: carousel.js, lightbox.js, restore-scroll.js, theme-toggle.js (тема без перезагрузки, см. «Смена темы/языка»), form-ui.js (см. «Форма без JavaScript»), contact-widget.js (см. «Виджет связи»), parallax.js (см. «Параллакс»), phone-intl.js (см. «Форма без JavaScript»), cookie-notice.js (см. «Уведомление о cookie»), route-progress.js (маршрут дня: линия-прогресс с инерцией поверх пунктира, загорающиеся точки .lit и серый микроавтобус .route-van на кончике линии (инлайн-SVG строится скриптом; вид сверху, капотом по ходу движения — вид сбоку и его поворот на 90° читались «едущим боком»; окна — вырезы fill-rule evenodd, цвет — var(--text-muted); у капота — фары `.route-lamp` (тёплые точки #ffe9a8) и конусы света `.route-beam` на дорогу (26.08.2026): лучи нарисованы в том же svg за пределами viewBox (в app.css на svg `overflow: visible`), поэтому при развороте поворачиваются вместе с кузовом и светят всегда вперёд; градиент гасит луч к концу, blur статичный, в светлой теме лучи приглушены opacity; при прокрутке вверх разворачивается капотом вверх — класс .rev, rotate на svg отдельным свойством: transition на transform обёртки тормозил бы позиционирование; за машинкой идёт выхлоп `.route-van-smoke` (26.08.2026) — четыре `.route-puff` с разбегом задержек: клуб — radial-gradient с прозрачной кромкой (плотные кружки читались «пузырьками», а не дымом — владелец), «выхлопывается» маленьким у трубы (труба чуть левее оси, у заднего бампера), быстро расширяется и рассеивается; снос у всех в одну сторону с вариацией `--puff-x`/`--puff-s` — «ветер» в разные стороны выглядел искусственно. Обёртка дыма разворачивается вместе с кузовом по `.rev`, видно его только на ходу — класс `.moving` скрипт снимает после ~12 кадров без движения; фото остановки плавно «наезжает» по мере приближения машинки — скрипт пишет li-переменную --stop-zoom (1..1.07, квадратичная близость в радиусе 340px), CSS применяет scale к img под html.js-route, рамка кадра режет зум overflow-ом); ярус 2 без JS — линию рисует CSS scroll-driven `route-draw`, при reduced-motion — статичный пунктир; позиции маркеров меряются offsetTop, а не getBoundingClientRect — reveal-каскад в момент замера держит карточки в transform) и effects.js (см. «JS-эффекты»). Новые скрипты — свои файлы по этому же образцу: reduced-motion и пауза вне вьюпорта обязательны (перфоманс и доступность — не про отсутствие JS), фоллбек — по желанию (с 28.08.2026, см. выше). Сторонние библиотеки допустимы (решение владельца от 19.08.2026 ради выразительности), но только вендоренные файлы в [js/vendor/](src/frontend/ui.public.web/wwwroot/js/vendor/) — CDN не пройдёт CSP `script-src 'self'`. Первая такая библиотека — Lenis 1.3.26 (инерционная прокрутка).
- **Стили только в [wwwroot/css/app.css](src/frontend/ui.public.web/wwwroot/css/app.css)**: CSP `style-src 'self'` запрещает и `<style>`, и inline-атрибуты `style=""` — оформление добавляется классами. Ссылка на app.css в App.razor несёт `?v=N` — поднимать при заметных правках стилей, иначе браузеры держат старую версию до часа (кеш css/js — час, fingerprinting нет; так уже делается с js-файлами). Дизайн-токены в `:root` (тёмная «полярная ночь», по умолчанию) и `[data-theme="light"]` («полярный день»). Тема переключается кнопкой в шапке: `/set-theme` ставит cookie `theme`, App.razor вешает `data-theme` на `<html>` и меняет `theme-color`. У параллакс-полос два комплекта слоёв: ночные [img/parallax-sky.svg](src/frontend/ui.public.web/wwwroot/img/parallax-sky.svg) + `parallax-hills-far/near.svg` и дневные с суффиксом `-light` для светлой темы (подмена фонов и цвета текста через `[data-theme="light"]`). Бейдж «Фото скоро» остаётся тёмным в обеих темах — он лежит на тёмной фото-заглушке, а не на фоне страницы.
- **Заголовки секций** (`.section-head`) — по центру во всех блоках (владелец, 25.08.2026): страница центрированная, и прижатые влево заголовки «что включено», «почему с нами», сезонов и FAQ выпадали из ритма. У блока мест, программы дня и формы свои правила с `max-width: none` — там заголовок во всю ширину контейнера. Блоки уже контейнера (`.faq-list` 860px, карточка формы 900px) обязаны нести `margin-inline: auto` — без него они прижимаются влево под центрированным заголовком, что и случилось с лентой вопросов (26.08.2026). Стык «программа дня → другие маршруты» ужат до 40+40px и живёт **после** адаптивных медиазапросов: `.section { padding-block }` из них перебивает правило, стоящее выше по файлу.
- **Меню разделов в шапке** ([Header.razor](src/frontend/ui.public.web/Components/Header.razor)): на десктопе (≥960px) — строка ссылок на якоря главной и аврора-кнопка «Записаться» (`.main-nav`, ключи `Nav*` общие с футером; по кнопке заявки в шапке — и по `.header-apply` на мобиле — раз в ~7 с пробегает световой блик `cta-shine`, только transform на `::after`, при reduced-motion выключен); уже 960px — бургер `.nav-burger` на нативном `<details>` с тем же паттерном закрытия, что у маркеров карты (fixed-оверлей в `summary::before`, transform на details запрещён) плюс кнопка `.header-apply` («Записаться»), а переключатель языка уезжает в бургер (`.burger-lang`) — заявка нужна всё время, язык меняют раз за визит (21.08.2026, взамен удалённой липкой полосы внизу). Иконка бота остаётся в шапке на всех ширинах (владелец, 24.08.2026; раньше на ≤430px уезжала в бургер строкой `.burger-bot` — удалена), на ≤380px кнопка заявки и бренд просто компактнее (полное «Записаться» и имя бренда остаются — владелец), текст бренда `.brand-name` прячется только на ≤358px (остаётся знак; на 360 край контролов ровно 358px) — иначе шапка шире экрана и у всей страницы появляется горизонтальная прокрутка (проверять `scrollWidth` на 320/360). Ссылки вида `/#itinerary` работают и со страниц мест. Состав пунктов в строке и в бургере одинаковый — «Териберка», «Другие маршруты», «Вопросы»: меню не должно зависеть от ширины экрана. Пункт «Программа дня» (`NavItinerary`, якорь `#itinerary`) убран владельцем 28.08.2026 из шапки и футера — ключ и сам блок на главной целы, ссылка возвращается одной строкой. **Порог строки меню — 1040px** (28.08.2026 его опускали до 960 после удаления «Программы дня», вернули с возвратом «Галереи»): строке нужно 975px вместе с брендом и контролами — измерено во всех трёх языках, русский самый широкий (en 889, zh 820), — плюс полоса прокрутки, то есть ниже ~992 опускать нельзя. **Порог обязан совпадать с парным `@media (max-width: 1039px)`**, который выводит `.header-apply` и прячет язык в бургер: пока они расходились (960 против 1039), на 960..1039px висели сразу две кнопки заявки, а переключатель языка уезжал в бургер, которого на этой ширине нет. Меняешь состав пунктов — перемеряй ширину шапки **во всех языках** и правь порог, иначе шапка вылезет за экран и даст горизонтальную прокрутку всей странице (проверять `scrollWidth`; так и было на 900–1000px при пяти пунктах, 26.08.2026). Пункт `NavPlaces` (якорь `#places`, блок «Что вы увидите») переименован из «Места» в «Териберка» (владелец, 26.08.2026) — в блоке только места Териберки, и рядом с «Другими маршрутами» название читается парой «основной маршрут / остальные»; ключ тот же, футер обновился вместе с шапкой. Ссылка «Галерея» убиралась 26.08.2026 и **возвращена 28.08.2026** по решению владельца: `/gallery` по-прежнему заглушка «скоро» (noindex, не в sitemap), но пункт в меню нужен — раздел наполнят позже. Стоит последним перед «Записаться», в шапке, бургере и футере. Примечание `.places-more` из блока мест при этом осталось убранным. Ключи `NavSeasons`/`NavGuides` остались от удалённых блоков, `NavItinerary` и `NavReviews` — от блоков, которые на странице есть, но пункта меню не имеют; все четыре сейчас без ссылок.
- **Кнопки прокрутки** (`.scrollers` в MainLayout) — якорные ссылки `#top`/`#page-bottom` + `scroll-behavior: smooth`; стрелки — инлайн-SVG (символы ↑↓ рисовались тонкими и терялись над водой морской зоны), кольцо и стрелка акцентного цвета, подложка почти непрозрачная, к тени добавлено аврора-свечение (владелец, 26.08.2026); «наверх» проявляется после первого экрана через CSS scroll-driven animation (`animation-timeline: scroll()`, в браузерах без поддержки видна всегда). На телефоне (≤699px) видна всегда ровно одна кнопка: «вниз» гаснет на том же отрезке 0–60vh, где проявляется «наверх» (владелец, 28.08.2026 — две кнопки столбиком мешали смотреть); `visibility` в keyframes переключается в самом конце, поэтому кнопка кликабельна, пока не растаяла; без поддержки scroll-timeline — обе, как раньше.
- **Смена темы/языка** (24.08.2026 — без «дёргания»): тему при работающем JS переключает [js/theme-toggle.js](src/frontend/ui.public.web/wwwroot/js/theme-toggle.js) **без перезагрузки** — отменяет навигацию по ссылке `/set-theme`, ставит `data-theme`/`theme-color`/иконку на месте (мягко, через `document.startViewTransition`) и фоново дёргает тот же endpoint fetch'ем ради cookie (она HttpOnly — из JS её не поставить; сервер остаётся единственным местом, где задаются атрибуты). Цвета WebGL-воды закешированы в uniform'ах — water.js перечитывает их по MutationObserver на `data-theme`. Плюс страховка позиции в theme-toggle.js: полсекунды после клика скрипт возвращает прокрутку на место, если она прыгнула больше чем на 40px (владелец ловил уход наверх при смене темы, 25.08.2026). **Ловушка, уже наступленная (26.08.2026)**: новый href ссылке ставится только `setAttribute` с относительным путём. Присвоение `link.href = …` пишет в атрибут абсолютный URL, а селекторы `a[href^="/set-theme"]` в theme-toggle.js и restore-scroll.js сравнивают именно атрибут — после первого клика ссылка переставала находиться, второй клик уходил в полную навигацию, и страница прыгала наверх. Язык без перезагрузки не сделать (тексты рендерит static SSR), но рефреш сглажен: [js/restore-scroll.js](src/frontend/ui.public.web/wwwroot/js/restore-scroll.js) запоминает позицию в sessionStorage перед кликом по `/set-theme`|`/set-culture` и восстанавливает после перезагрузки, а effects.js **подглядывает** (не удаляя) его ключ и не выдаёт reveal-каскад блокам, которые окажутся в восстановленном вьюпорте — уже прочитанное не «выезжает» заново. Порядок скриптов в MainLayout важен: theme-toggle.js подключён после restore-scroll.js, чтобы подчистить его запись по обработанному клику. Без JavaScript всё работает по-старому: ссылки, рефреш, переход наверх.
- **Локализация**: ru (по умолчанию, нейтральный resx) / en / zh. Строки — в [Resources/SharedResource*.resx](src/frontend/ui.public.web/Resources/), в разметке — `IStringLocalizer<SharedResource>`. Культура хранится в cookie; переключатель — GET-ссылки на `/set-culture?culture=xx&redirect=...` (endpoint в Program.cs валидирует культуру по белому списку и делает LocalRedirect). Accept-Language сознательно не учитывается. Тексты ошибок FluentValidation в контрактах — только русские.
- **Страницы мест**: карточка на главной — ссылка на `/place/{slug}` ([PlaceView.razor](src/frontend/ui.public.web/Features/Places/Pages/PlaceView.razor)). Каталог мест — статический [PlaceCatalog](src/frontend/ui.public.web/Features/Places/PlaceCatalog.cs) (это контент, БД не нужна): новое место = запись в каталоге + ключи `Place{i}Title/Text/Detail1/Detail2/Detail3/Fact` в трёх resx. Места сгруппированы в **направления** (`PlaceCatalog.Directions`: Териберка / Тундры и Рыбачий / Терский берег; ключи `Dir{i}Title/Text`, `MapSpot{i}`; `MapSpot2` намеренно остался «Ловозерские тундры» — маркер стоит на их месте, а Рыбачий на карте — своя аврора-точка) — Териберка на главной идёт до формы, два других направления — после неё; маркеры карты те же. Неизвестный slug отвечает честным 404 через `Navigation.NotFound()` — роутер рендерит `NotFoundView` (ловушка: ручной `Response.StatusCode = 404` в static SSR отдаёт 404 с **пустым телом**, поэтому только `NotFound()`). URL, не совпавший ни с одним `@page` (нет endpoint'а), добирает `UseStatusCodePagesWithReExecute("/not-found")`; в `NotFoundView` guard по уже выставленному 404 не даёт зациклиться. `PlaceCatalog.BySlug` сознательно регистрочувствителен — иначе `/place/UMBA` был бы 200-дублем канонического URL.
- **Карта направлений** ([KolaMap.razor](src/frontend/ui.public.web/Components/KolaMap.razor)) живёт в hero (текст слева, карта справа; на мобиле — столбиком). Контур Кольского — инлайн-SVG по реальной географии (краски из CSS-переменных, поэтому темы работают), подписи морей (`MapSeaNorth/South`), расстояния на маршрутах в единицах языка (`MapDist{i}`: км для ru/zh, мили для en). Три больших маркера направлений — нативные `<details>`-попапы; малые аврора-точки мест (`.map-place-*`) — прямые ссылки на `/place/{slug}` (подписи слева от точки, кроме «Каменного лабиринта»: слева она вылезала за левый край карты — с 25.08.2026 у него подпись справа, а сам маркер приподнят до 75%, чтобы не задевать «Умбу»); города (Мурманск, Кировск, Апатиты) — жёлтые точки в SVG; аэропорты Мурманска и Хибин (у Апатитов) — точки встречи туристов: плоский самолётик `.poi-icon` цветом текста (без кольца — решение владельца) виден на всех экранах, подпись «Аэропорт» — только на широких, тултип `MapAirportHint` («встречаем в аэропорту и в городе»); некликабельны. Подпись «Белое море» стоит по центру x=420 — правее она уезжала к краю карты. Попап закрывается кликом в любом месте: невидимый `position: fixed`-оверлей — это `summary::before`. Пасхалка `.map-whale`: кит живёт в одном месте — прямо над маркером Териберки — и раз в 45 с всплывает, чуть дрейфует и уходит под воду (keyframes `whale-surface`; само всплытие длится те же ~9 с, при цикле 18 с кит мозолил глаза — владелец, 25.08.2026; прежний маршрут из четырёх точек вдоль берега убран владельцем 21.08.2026); на ≤560px приподнят через `translate` (в единицах viewBox), потому что HTML-маркер в масштабе SVG крупнее и задевал его; при `prefers-reduced-motion` скрыт. Фонтан и падающая комета в hero были — убраны сознательно. **Ловушка**: на `.map-spot` нельзя вешать `transform`/`translate` — трансформ делает fixed-оверлей локальным (containing block), поэтому маркеры-details позиционируются `left/top: calc(% - Npx)`.
- **Логотип** ([favicon.svg](src/frontend/ui.public.web/wwwroot/favicon.svg)) — силуэт Кольского полуострова в аврора-градиенте (тот же path, что на карте) с жёлтой точкой Мурманска и упрощённым глифом скелета левиафана внутри (детальный скелет на 28px не читается — поэтому именно упрощённый: череп-овал, 4 ребра, пунктир позвоночника); используется и как favicon, и как brand-mark в шапке/футере. Тёмной плашки-подложки нет — на светлой теме она выглядела инородно.
- **Фото мест**: карточки на главной, большое фото и галерея-карусель страницы места — реальные снимки `wwwroot/img/places/{slug}.webp` и `{slug}-g1..gN.webp` (1280×853, 3:2). Главные фото пяти мест Териберки — **собственные кадры владельца** (26.08.2026, оригиналы в `src/frontend/temp`: IMG_6565 → dragon-eggs, IMG_6102 → ship-graveyard, IMG_6862 → batareysky-waterfall, IMG_6946 → barents-sea, IMG_6910 → arctic-tundra); у них `Photo` в каталоге не задан — атрибуция нужна только чужим снимкам. Исключение — `northern-lights`: своего кадра с авророй у владельца нет, там пока фото с Commons. В `src` карточек и большого фото — `?v=2`: файлы перезаписаны под теми же именами, а картинки кэшируются 30 дней. Галереи мест и остальные направления — по-прежнему Wikimedia Commons. Кредиты (автор/лицензия/ссылка) лежат в `PlaceCatalog` (`PhotoCredit`/`Gallery`) и **показываются** (24.08.2026, раньше прятались как демо): компонент [PhotoCreditLine.razor](src/frontend/ui.public.web/Components/PhotoCreditLine.razor) — «Фото: автор · лицензия · обрезано» со ссылками на файл Commons и текст лицензии ([PhotoLicenses](src/frontend/ui.public.web/Features/Places/PhotoLicenses.cs)) — стоит под главным фото и под галереей страницы места; сводная страница `/photo-credits` ([PhotoCreditsView.razor](src/frontend/ui.public.web/Features/Legal/Pages/PhotoCreditsView.razor), ссылка в футере, есть в sitemap) собирается из каталога автоматически. Лицензии CC требуют атрибуции — подписи не убирать, пока фото чужие; заменишь кадр на свой — просто оставь `Photo`/`Gallery` пустыми. Заголовок галереи на странице места — ключ `PlacePhotosTitle` («Фотографии»): раньше он назывался `GalleryTitle`, совпадал с ключом раздела «Галерея» и молча им перебивался — дублей `name` в resx быть не должно, побеждает первый по файлу. Галерея — карусель на scroll-snap с целыми кадрами (1 на мобиле / 2 на десктопе) и стрелками из [js/carousel.js](src/frontend/ui.public.web/wwwroot/js/carousel.js) (обслуживает любой `.carousel-wrap` + `.carousel-track`; без JS стрелки скрыты, свайп работает; `data-autoplay="<мс>"` на обёртке включает автопрокрут — hero-карусель листается сама раз в 5 с, пауза вне вьюпорта/в фоновой вкладке/под курсором, любое ручное листание выключает его насовсем, при reduced-motion не стартует); клик по кадру — полноэкранный лайтбокс ([js/lightbox.js](src/frontend/ui.public.web/wwwroot/js/lightbox.js), фоллбек без JS — открытие файла в новой вкладке). У места без `Gallery` — четыре SVG-заглушки [img/placeholder.svg](src/frontend/ui.public.web/wwwroot/img/placeholder.svg) с бейджем «Фото скоро», разведённые CSS-фильтрами (фильтры ограничены `.gallery-grid`).
- **«Морская зона»** `.sea-zone` (MainLayout, на всех страницах) = полоса `.shore-band` + прозрачный футер на фоне воды. Слои: пологие скалы по краям (z0, инлайн-SVG, токен `--rock`; острые вершины со «снегом» читались как горы — не возвращать) → полоса (z1) → WebGL-вода на всю зону (z2, water.js; калибровка в px через uniform dpr, поверхность ~60px от верха зоны; спарклы убраны — читались как дождь) → скелет кита ([img/leviathan.svg](src/frontend/ui.public.web/wwwroot/img/leviathan.svg)) на дне: `.sea-zone::after` поверх воды с opacity .3 → стая касаток `.orca-pod` (тоже z2, инлайн-SVG [OrcaSilhouette.razor](src/frontend/ui.public.web/Components/OrcaSilhouette.razor) — именно инлайн, чтобы отдельный path хвоста мог махать анимацией `orca-tail`): раз в ~75 с три силуэта разного размера проплывают справа налево, анимируется только transform, «ожидание» спрятано за правым краем через overflow самой зоны → контент футера (z3). Фоллбек без JS — три слоя SVG-волн с разными периодами, плывущие навстречу бесшовным `translate3d`-циклом. Рябь SVG-фильтром (feTurbulence) сознательно не делаем — фильтр рендерится CPU на каждый кадр. Миниатюра кита лежит на берегу в parallax-hills-near.svg; в hero скелет класть не стоит — уже пробовали, конфликтует с контентом.
- `<title>` задаётся только через `<PageTitle>` (HeadOutlet); статического title в App.razor нет — иначе в head оказываются два тега.
- **SEO** (языковые префиксы URL сознательно не делаются — культура в cookie, краулеры видят русскую версию, canonical всегда без культуры):
  - `SITE_URL` (конфиг UI) — база абсолютных URL. Пустой → canonical/og:url/og:image не рендерятся, `/sitemap.xml` отдаёт 404, robots.txt — без строки `Sitemap`. Логика — в [Features/Seo/SeoUrls.cs](src/frontend/ui.public.web/Features/Seo/SeoUrls.cs) (он же лениво собирает robots/sitemap; endpoints в Program.cs объявлены **до** `MapRazorComponents`, иначе упадут в Razor-роутер). Новое место в `PlaceCatalog` попадает в sitemap автоматически.
  - **Контракт [PageMeta.razor](src/frontend/ui.public.web/Components/PageMeta.razor)**: глобального `<meta name="description">` в App.razor больше нет — **каждая индексируемая страница обязана рендерить `<PageMeta>`** (description, canonical, OG/Twitter; `Path` — чистый канонический путь, поэтому `?sent=true`/`?tour=custom` канонизируются сами), а 404-страницы — `<PageMeta Noindex />`. Заголовки мест: `{Place} — PlaceTitleSuffix · Brand`; в og:title — голое название (карточки в мессенджерах читаются лучше короткими).
  - JSON-LD — [JsonLd.razor](src/frontend/ui.public.web/Components/JsonLd.razor) (словарь → `<script type="application/ld+json">`; CSP не мешает — это data block, не исполняемый скрипт; в значения класть `L[...].Value`). На главной — FAQPage + WebSite, на местах — TouristAttraction. Контакты/логотип в разметку сознательно не добавлены: в футере плейсхолдеры, а логотип schema.org требует растр ≥112px.
  - Ссылки `/set-culture`/`/set-theme` в шапке — `rel="nofollow"`, в robots.txt те же пути в `Disallow`.
  - Пост-запуск чек-лист: верификация в Search Console и Яндекс.Вебмастере + отправка sitemap; растровая OG-картинка/логотип 1200×630; реальные контакты прежде, чем добавлять их в JSON-LD.
- Статика раздаётся `UseStaticFiles()` **до** rate limiter'а — css и картинки не сжигают лимит. Кэш: css/js — час (fingerprinting нет), картинки — 30 дней. Сжатие (`UseResponseCompression`) — только css/js/svg/xml/plain: `text/html` сознательно не сжимается (antiforgery-токен + отражённый query в ссылках set-culture — BREACH), webp сжат кодеком.
- Ловушка биндинга: `[SupplyParameterFromQuery] bool` понимает только `true`/`false` — редирект после формы обязан быть `/?sent=true`, с `?sent=1` страница «спасибо» молча не покажется.
- **Параллакс** `.parallax-band` — многослойная сцена, три яруса реализации (все описаны в app.css, секция «параллакс-полосы»). Слои: небо `parallax-sky.svg` с `background-attachment: fixed` (iOS игнорирует) → дальняя гряда `parallax-hills-far.svg` (`::before`; светлее — атмосферная перспектива, с тонкой аврора-подсветкой гребня) → туман `.parallax-mist` (div в разметке; тайл [img/mist.svg](src/frontend/ui.public.web/wwwroot/img/mist.svg) бесшовно дрейфует вбок циклом `translate3d` ровно на ширину тайла; из-за порядка отрисовки — `::after` идёт после детей — туман оказывается между грядами, в долинах) → ближняя гряда `parallax-hills-near.svg` (`::after`; вода и кит-пасхалка) → текст. Силуэты сопок — плавные кривые Безье (треугольные вершины смотрелись примитивно — не возвращать), viewBox 1920×560, нижняя привязка `background-position: center bottom`; светлая тема — те же слои с суффиксом `-light` (25.08.2026 их палитра углублена, а белёсая заливка поверх неба и повышенная непрозрачность тумана убраны: днём сцена читалась пустым местом). Ярусы: (1) база — слои статичны, глубину даёт fixed-небо, работает везде; (2) `@supports (animation-timeline: view())` — слои едут от прокрутки чистым CSS, амплитуда `--parallax-shift` (плюс = отстаёт/дальше, минус = обгоняет/ближе); (3) [js/parallax.js](src/frontend/ui.public.web/wwwroot/js/parallax.js) вешает `html.js-parallax` (CSS-анимации яруса 2 выключаются) и каждый кадр пишет на полосу переменные `--p-*` — инерционный ход (lerp) плюс горизонтальный отклик на мышь на `pointer: fine`; расчёт только пока полоса видна, при `prefers-reduced-motion` скрипт не стартует. Ловушка: запас слоя (отрицательный `inset`) обязан превышать амплитуду хода по обеим осям, иначе на краях оголяется фон.
- **JS-эффекты** — [js/effects.js](src/frontend/ui.public.web/wwwroot/js/effects.js) (вешает `html.js-fx`; при `prefers-reduced-motion` не стартует): (1) инерционная прокрутка — вендоренный Lenis (`js/vendor/lenis.min.js`, `anchors: true`, тач не трогает; рекомендованные стили Lenis — в app.css, секция «js-эффекты»); (2) параллакс-слои `data-speed` по образцу andyhardy.co — элемент отстаёт от прокрутки на долю speed (в hero: `.hero-copy` 0.07, `.hero-map` 0.16), расчёт только около вьюпорта; не совмещать `data-speed` и `.reveal` на одном элементе — оба пишут transform; (3) пружинящие reveal-каскады: селекторам карточек и `.section-head` скрипт ставит `.reveal`/`.appear`, transition-delay по индексу среди соседей (элементы внутри реально прокручиваемого `.carousel-track` — карточки мест на телефоне — каскад не получают: кадр «выезжал снизу» при листании, а все карусели должны вести себя одинаково), CSS-подъём `card-rise` под `html.js-fx` выключен, без JS ничего не прячется; (4) морф-имена для view transitions (ниже).
- **Переходы между страницами** — кросс-документные View Transitions чистым CSS (`@view-transition { navigation: auto }` в app.css): кроссфейд ~0.4 c; Firefox без поддержки просто переходит как обычно, при reduced-motion анимации отключены. Фото места «перелетает» между карточкой главной и страницей места: одинаковый `view-transition-name: place-{slug}` на обоих `<img>` ставит effects.js через CSSOM (инлайновый `style=""` запрещён CSP, `el.style` — нет). Обратный морф (место → главная) может деградировать в кроссфейд: карточки на главной в момент снапшота ещё скрыты reveal-каскадом — это осознанный компромисс.
- **WebGL-«жидкое» фото при наведении** — [js/photo-fx.js](src/frontend/ui.public.web/wwwroot/js/photo-fx.js) (по образцу water.js, без библиотек): один общий канвас `.photo-fx` (fixed, pointer-events: none, z-index 20 — выше карточек, ниже метели и лайтбокса) накладывается поверх фото под курсором (`.place-photo img`, `.detail-photo img`); шейдер даёт рябь от курсора, лёгкий зум и хроматическую аберрацию, сила лерпится 0↔1, при нулевой силе кадр совпадает с исходным фото (cover-маппинг в uv). Только `pointer: fine`, при reduced-motion не стартует, без WebGL остаётся обычный CSS-hover. Ловушка: текстуру НЕ грузить как NPOT без мипмапов — минификация LINEAR даёт зернистый алиасинг; фото пережимается в POT 1024² и включается `LINEAR_MIPMAP_LINEAR` (аспект не важен, uv нормализованы).
- **Отзывы** (`.reviews`, id=reviews) — возвращены владельцем 28.08.2026 (убирались как вымышленный контент): шесть карточек из девяти сохранённых ключей `Review1..9`, стоят **после «Что важно знать» (FAQ), перед финальным призывом** (владелец, 28.08.2026 — сначала переставлялись между «почему с нами» и FAQ). Состав — массив `Reviews` в Home.razor (id ключа + звёзды + дата): id 2, 1, 5, 4, 7, 8 — **только гости из России** (владелец, 28.08.2026): китайцы Ван Лэй-3 и Чжан Вэй-9 и Сара из Лондона-6 не показываются (ключи в resx целы), у Павла-8 город сменён с Минска на Нижний Новгород, а текст Елены-4 переписан — прежний дублировал Дмитрия (драконьи яйца и уха). У Игоря (id 5) четыре звезды — его текст прямо говорит «звезду снял». Под звёздами — месяц поездки (28.08.2026): дата лежит в том же массиве (`DateOnly`) и рендерится `ToString("y")` — формат «месяц + год» культуры запроса, ключей в resx не нужно; **все даты — в текущем году и в прошлом** (владелец, 28.08.2026: январь–июль 2026), подогнаны под сюжет отзыва (аврора — только зимой и ранней весной) и разведены по месяцам. **Тексты и даты — черновики до замены на настоящие отзывы.** Что нужно для настоящих (обсуждалось с владельцем 28.08.2026): собирать отзыв просьбой после тура (бот/чат гидов), хранить согласие гостя на публикацию имени и особенно фото (изображение гражданина — ст. 152.1 ГК), а как проверяемый источник заводить карточку в Яндекс.Картах и 2ГИС и вести отзывы туда. **JSON-LD для отзывов не добавлять и с настоящими**: `Review`/`AggregateRating` на собственном сайте организации Google считает self-serving — звёзд в выдаче не даст, а за вымышленные накажет; рейтинг в выдаче даёт профиль на картах. Лента — тот же `.carousel-wrap` + scroll-snap, что у мест: свайп без JS, стрелки от carousel.js, на десктопе три карточки в кадре; автопрокрута сознательно нет — отзыв читают дольше, чем смотрят фото. Над карточками — **фотолента гостей** `.guests-row` (владелец, 28.08.2026): кадры-«полароиды» с лёгким разворотом (rotate на `.guest-shot` безопасен — details-оверлеев внутри нет), клик — лайтбокс `a.lightbox-link`, на мобиле (≤799px) — по одному кадру во всю ширину со стрелками carousel.js (край соседнего кадра владелец счёл обрезом, 28.08.2026 — та же претензия, что была к ленте мест; правило трёх классов `.guests-carousel.carousel-wrap .car-btn` прячет стрелки на десктопе), от 800px — центрированный ряд с переносом. Файлы — `wwwroot/img/guests/guest-*.webp` (кроп 3:4, convert-photos.py), рендерится содержимое каталога: **нет файлов — нет ленты**, новый кадр = новый файл без правки кода; `?v=` из mtime файла — перезаливка не застревает в 30-дневном кеше. В каталоге 4 кадра владельца (28.08.2026, оригиналы в `src/frontend/temp/guests/`): пара с морошкой → группа за столом → гребешок над водой → изба с медвежьей шкурой; порядок «сначала люди, потом фактура». Лица гостей — помнить про согласие на публикацию (ст. 152.1 ГК). Фото гостей владелец пришлёт позже: файл `wwwroot/img/reviews/review-{id}.webp` (id — номер ключа, не позиция в ленте) подхватывается сам (`File.Exists` в Home.razor), пока его нет — кружок-инициал `.review-avatar`. В JSON-LD отзывы сознательно не добавлены (разметка вымышленных отзывов — прямой путь под санкции поисковиков). Пункта в меню шапки нет: `NavReviews` в resx остался, но добавление пункта требует перемерить ширину шапки (см. порог 1040px выше).
- **Микроанимации** (мерцание звёзд и дрейф авроры в hero) анимируют только `opacity`/`transform` — исполняются GPU-композитором. При добавлении новых анимаций держаться этого правила (не анимировать background/box-shadow/filter) и не забывать ветку `prefers-reduced-motion`.
- **Метель** (`.blizzard` в MainLayout) — fixed-слой поверх всего сайта: порыв ~14 с раз в ~40 с. «Таймер» — длинный keyframes-цикл opacity на контейнере; движение снега — бесшовный диагональный `translate3d` слоёв ::before/::after ровно на размер тайла паттерна (слои больше вьюпорта на тот же тайл, чтобы края не оголялись). Цвет снега — `--snow-rgb` (в светлой теме серо-голубой). При `prefers-reduced-motion` слой скрыт целиком.
- Скриншоты для проверки вёрстки: headless Chrome на Windows не отдаёт окно уже ~500px, поэтому мобильную ширину снимать через CDP `Emulation.setDeviceMetricsOverride` (+`Page.captureScreenshot` с `captureBeyondViewport`), а не `--window-size`. На таких скриншотах fixed-фоны параллакс-полос выглядят пустыми — это артефакт съёмки, вживую фон на месте. Вторая ловушка съёмки: при `captureBeyondViewport` карточки под reveal-каскадом effects.js остаются с opacity 0 (IntersectionObserver за вьюпортом не срабатывает) — блок выглядит пустым; лечится эмуляцией `prefers-reduced-motion: reduce` через CDP (effects.js тогда не стартует) либо окном в полную высоту страницы.

### Форма без JavaScript

Фронтенд работает на **static SSR** — интерактивный render mode не зарегистрирован и `blazor.web.js` не подключён, поэтому `@onclick`/`@onsubmit` в браузере не выполняются. Форма серверная: `EditForm` + `[SupplyParameterFromForm]` + `FormName`, обработка на POST, после успеха — редирект на `/?sent=true` (post-redirect-get), чтобы F5 не отправлял заявку повторно. Форма одна — компонент [ApplicationForm.razor](src/frontend/ui.public.web/Components/ApplicationForm.razor); ветка `Custom` (`/?tour=custom#apply`) меняет кикер/заголовок/подводку и предвыбирает маршрут `custom`, а POST, FormName и обработка общие — отдельных форм не плодить. Ссылок на эту ветку со страницы больше нет (блок «Индивидуальный тур» убран 25.08.2026), но сама она рабочая. Кикер, заголовок и подводка живут **не в карточке, а в шапке секции** (`.section-head.apply-head` в Home.razor, 25.08.2026: у формы должен быть такой же белый заголовок над карточкой, как у остальных блоков); после отправки шапка не рендерится — там уже карточка «спасибо» со своим заголовком. Список «что будет после заявки» — четыре пункта `FormPoint1..4` в две колонки от 700px (три оставляли дыру справа, владелец 25.08.2026). Поля — `Title` (имя, необязательно), `Phone` (обязателен) и `Route` (маршрут, по умолчанию Териберка; миграция `AddApplicationRoute`, 21.08.2026): поля даты, числа людей и пожеланий убраны из контракта, сущности и БД (миграция `RemoveApplicationDetails`, 21.08.2026) — не возвращать без решения владельца. Маршрут хранится **кодом** (`teriberka` / `lovozero` / `tersky` / `custom`, константы в [ApplicationRoutes](src/contracts/Applications.Contracts/ApplicationRoutes.cs)), а названия берутся из resx — `MapSpot1..3` и `RouteCustomTitle`; переименование маршрута не требует миграции; валидатор принимает только известные коды, иначе подделанный POST записал бы в заявку произвольную строку. **Новый код маршрута — это правка контрактов, общих с бэком**: тот же валидатор работает в users через `ValidatorBehavior`, поэтому без пересборки контейнера (`docker compose up -d --build --force-recreate api-users`) сайт покажет «Выберите маршрут из списка» на новый вариант. `--build` без `--force-recreate` образ пересобирает, а контейнер оставляет старый — проверено 25.08.2026. Выбор маршрута — **карточки-радиокнопки, а не `<select>`** (владелец, 25.08.2026): у каждого варианта видно описание (`Dir{i}Text`, у индивидуального — `RouteCustomText`), а подменять текст под выбранный пункт без JavaScript нельзя, поэтому все описания показаны сразу. Инпуты обычные (`<input type="radio">`), не `InputRadio`; **имя поля обязано нести префикс модели — `Model.Route`** (так же называет поля `InputText`), иначе выбор молча не доезжает до сервера, а валидатор ругается на пустой маршрут. Подсветка выбранной карточки — `input:checked + .route-option-box`, без `:has()`. Ветка `Custom` (`/?tour=custom#apply`) заодно предвыбирает маршрут `custom`. Фото-шапки у карточки формы больше нет (владелец, 25.08.2026) — вместе с ней удалены `.apply-photos`; кнопка отправки со своей сноской собрана в колонку `.form-submit-main`, симметричную кнопке бота, а сам ряд на десктопе центрируется (`justify-content: center`) — прижатый влево он оставлял дыру справа. Карточка формы шире прежнего (900px против 720): узкая колонка под трёхколоночной лентой мест смотрелась случайной, но во всю ширину контейнера (1080) её не растягиваем — подводка ушла бы за сотню символов в строке, поэтому у `.apply-lead` ещё и `max-width: 62ch`. **Якорь `#apply` висит на самой форме** (и на карточке «спасибо»), а не на секции: ссылки «Записаться» должны приводить к первому полю, а не к шапке карточки; `scroll-margin-top` — на `.apply-form`/`.sent-card`. Antiforgery-токен `EditForm` добавляет сам, `app.UseAntiforgery()` включён. **Капча и JS**: при заданных ключах SmartCaptcha (см. «Капча» в Безопасности) отправка требует работающий JS — токен собирает скрипт; без ключей (сейчас так) форма работает и без JS. Правило «всё без JS» снято владельцем 28.08.2026, так что это штатное поведение, а не исключение. **Телефон — международный** (владелец, 26.08.2026): слева выпадающий список стран с флагом и кодом, справа сам номер, значение по-прежнему одна строка `Model.Phone` — контракт и валидатор не менялись. Список — [PhoneCountries](src/frontend/ui.public.web/Features/Contacts/PhoneCountries.cs) (27 стран: Россия по умолчанию, дальше Китай, Норвегия, Финляндия, Великобритания, Франция, остальная крупная Европа, СНГ и Латинская Америка; полного справочника мира сознательно нет — чужой код набирается руками с «+», маска такой номер не трогает). Названия стран лежат в самом каталоге тремя строками (`NameRu/NameEn/NameZh`), а не в resx: это справочные данные, сотня служебных ключей их не стоит. Флаги — инлайн-спрайт [FlagSprite.razor](src/frontend/ui.public.web/Components/FlagSprite.razor) (`<use href="#flag-{iso}">`): внешний SVG в `<use>` браузеры не грузят, а emoji-флаги Windows не рисует вовсе; рисунок упрощён (гербы Испании, Португалии и Мексики — кружок), новая страна в каталоге требует symbol с тем же iso. Логика — [js/phone-intl.js](src/frontend/ui.public.web/wwwroot/js/phone-intl.js). **Код в поле не дублируется** (владелец, 26.08.2026: «+7» в плашке и «+7» в номере читались как два кода): в поле только национальная часть («912 345-67-89»), плейсхолдер тоже без кода, а полный «+7 912 345-67-89» скрипт собирает в **capture-фазе submit** — она срабатывает раньше bubble-валидации form-ui.js независимо от порядка подключения скриптов, поэтому валидатор и сервер видят номер с кодом; если валидация не пустила форму, таймер возвращает полю национальный вид. Вставка «+33…» уводит код в плашку и переключает страну (самый длинный совпавший код), «8 912…» теряет приставку `Trunk`, «7912…» длиннее национального — лишний код, номер с чужим «+» остаётся в поле целиком и на submit не трогается. Формат — по группам страны (`Groups` — размеры и разделители, «3 3-2-2» → «912 345-67-89»), курсор возвращается к той же цифре. Данные скрипт читает только из `data-*` пунктов — единственный источник списка остаётся в C#. Селектор — **прогрессивное улучшение**: показывает его класс `js-phone` на `<html>`, который вешает сам скрипт, поэтому без JavaScript остаётся прежнее поле с кодом, набранным руками. Поле телефона — единственное собрано на `div` + `label for`, а не на `<label>`-обёртке: клик по `details`/кнопкам внутри label уводил бы фокус в input. Закрытие списка — тот же fixed-оверлей в `summary::before`, что у бургера, значит `transform`/`filter` на `.phone-cc` вешать нельзя. Валидация: [js/form-ui.js](src/frontend/ui.public.web/wwwroot/js/form-ui.js) ставит форме `novalidate` (маска переехала оттуда в phone-intl.js), проверяет телефон (та же regex, что в валидаторе, **плюс длина**: 11 цифр у номера на «+7», у остальных 8..15 по E.164 — иначе «+7 900» проходило бы серверную regex обрывком) и согласие и показывает подсказки `.field-hint-error` под полями (тексты — `data-val-*` на форме из resx); к первому полю с ошибкой страница плавно прокручивается (`scrollIntoView block: center`, при reduced-motion — рывком; фокус — с `preventScroll`, чтобы не дёргать ход) — кнопка внизу формы, и без скролла клик выглядел «не работающим» (владелец, 28.08.2026); без JS — HTML5-атрибуты и системные пузыри; на сервере — FluentValidation-валидатор из контрактов (в UI внедряется как `IEnumerable<IValidator<T>>`, ошибки рендерятся теми же классами), плюс тот же валидатор отрабатывает в сервисе через `ValidatorBehavior`. Стили ошибок — мягкая подсветка поля в `--danger` + подсказка с маркером «!»; чекбокс согласия — свой (`appearance: none`, галочка из `::after`, аврора-фон при `:checked`), нативный в тёмной теме выглядел инородно.

При переходе на интерактивный рендеринг (`AddInteractiveServerComponents` / `AddInteractiveServerRenderMode`) форму придётся переписать под обычные обработчики событий.

### Контракты общие для бэка и фронта

`src/contracts/*` ссылаются и сервисы, и Blazor-проект. Один и тот же тип (`ApplicationCreateCommand`) — это одновременно Mediator-запрос в UI, Mediator-запрос в сервисе и тело `[FromBody]` у HTTP-эндпоинта. Базовые типы в [src/contracts/Contracts/](src/contracts/Contracts/): `Command`, `DeleteCommand`, `Query<TDto>`, `ListQuery<TDto>`, `PagedListQuery<TDto,TQuery>`, `Dto`, `AuditableDto` и базовые валидаторы.

Следствие: **чтобы UI мог вызвать эндпоинт, в `ui.public.web/Handlers` нужен свой `ApiHandler`-наследник**, иначе Mediator не найдёт обработчик (генератор предупреждает `MSG0005`, а `Send` падает в рантайме).

### Обобщённый CQRS-конвейер: код фичи — почти только типовая обвязка

Всё реализовано в generic-виде в `src/backend/shared`, поэтому фича обычно = набор пустых наследников:

- Handler'ы: `CreateCommandHandler<>`, `UpdateCommandHandler<>`, `DeleteCommandHandler<>`, `SingleQueryHandler<>`, `ListQueryHandler<>`, `PagedListQueryHandler<>`.
- Репозитории `CommandRepository<,>`, `SingleQueryRepository<,,>`, `ListQueryRepository<,,>` зарегистрированы как open generic в `AddApiServices`. Свой репозиторий нужен только для нестандартной выборки — тогда наследуешься и переопределяешь `BuildDbQuery` / `ProcessDbQuery` / `ProcessTextQuery` / `ProcessSorting` / `ProjectSimple`; Scrutor подхватит наследника автоматически и он победит open-generic регистрацию (пример: [ApplicationListQueryRepository](src/backend/users/modules/Users.Infrastructure/DataAccess/Repositories/Application/ApplicationListQueryRepository.cs) сортирует заявки свежими сверху).
- Маппинг: Mapperly, регистрация по соглашению об имени — класс должен называться `*DtoMapper` / `*EntityMapper` / `*RpcMapper` и лежать в сборке, имя которой заканчивается на `Infrastructure.dll` (`ConfigureMappers`). Переименуешь — маппер молча не зарегистрируется.
- Валидация: `ValidatorBehavior<,>` подключается в `Configurator.ConfigureDependencies`, валидаторы находятся по сборкам автоматически.

Сквозной референс — сущность `Application` (заявка): [contracts](src/contracts/Applications.Contracts/), [entity](src/backend/users/modules/Users.Domain/Entities/Application.cs), [EF config](src/backend/users/modules/Users.Infrastructure/DataAccess/Config/ApplicationConfig.cs), [mappers](src/backend/users/modules/Users.Infrastructure/DataAccess/Mappers/ApplicationMapper.cs), [handlers](src/backend/users/modules/Users.Application/Handlers/Application/), [эндпоинты](src/backend/users/service/Configurator.cs), [UI handler](src/frontend/ui.public.web/Handlers/ApplicationHandlers.cs), [страница с формой](src/frontend/ui.public.web/Features/Home/Pages/Home.razor).

Чек-лист новой сущности: контракт → entity → EF config → мапперы → handler'ы → эндпоинт в `ConfigureEndPoints` → **маршрут в шлюзе** (если нужен внешний доступ; новому сервису нужен ещё и **кластер**) → миграция → handler + страница в UI. Забытый маршрут в шлюзе — самая частая причина «эндпоинт есть, но недоступен».

Нестандартный случай — чат ([Chat.Contracts](src/contracts/Chat.Contracts/), chat-сервис): там сообщение нельзя создать одним маппингом из команды, поэтому вместо generic `CommandRepository` написан свой [ChatRepository](src/backend/chat/modules/Chat.Infrastructure/DataAccess/Repositories/ChatRepository.cs). Он не наследует ни один из базовых репозиториев, а значит **Scrutor его не найдёт** — регистрируется руками в `Configurator`.

### URL-конвенция и авторизация

Формат `/api/{role}/{group}/{action}`, где `role` ∈ `public` | `private` | `admin` (`Api.Constants.UrlRestrictions`). Эндпоинты объявляются fluent-билдером:

```csharp
app.MediateGroup("application", Constants.UrlRestrictions.Admin)
   .List<ApplicationListQuery, ApplicationDto>();              // GET /api/admin/application/list
app.MediatePostCommand<ApplicationCreateCommand>("application", "create");  // POST /api/public/application/create
```

Действия билдера: `Single` → `get`, `List` → `list`, `PagedList` → `pagedList`, `Create` → POST `create`, `Update` → PUT `update`, `Delete` → DELETE `delete/{id}`.

**Роль в URL — только сегмент пути, сервис её не проверяет.** Аутентификация и авторизация целиком на шлюзе: JWT bearer + политика `Admin` (claim role = 1/2, см. `UserRole`), назначаемая маршруту через `AuthorizationPolicy` в [gateway/appsettings.json](src/backend/gateway/appsettings.json). Отсюда требование к развёртыванию: **users-сервис не должен быть доступен из внешней сети** — сам он никого не проверяет и доверяет вызывающему. В `docker-compose.yml` его порт 5012 опубликован только для локальной отладки.

### Уведомление о cookie

Плашка внизу экрана ([CookieNotice.razor](src/frontend/ui.public.web/Components/CookieNotice.razor), подключается в `MainLayout` последней) — владелец, 26.08.2026.

- **Решает сервер, а не скрипт**: пока нет cookie `cookie_notice`, компонент рендерится, после согласия — не рендерится вовсе. Поэтому плашка не «моргает» на каждой странице и не показывается второй раз без JavaScript.
- Кнопка «Хорошо» — ссылка на endpoint `/accept-cookies` (cookie ставит только сервер, как у `/set-theme` и `/set-culture`; путь закрыт в robots.txt). С работающим JS [cookie-notice.js](src/frontend/ui.public.web/wwwroot/js/cookie-notice.js) отменяет переход, уводит плашку вниз и дёргает тот же endpoint фоновым fetch — без перезагрузки.
- Плашка **по центру экрана** (владелец, 26.08.2026), а не у края: `left`/`right` по 12px, `max-width` и `margin-inline: auto` — центрировать через `transform` нельзя, он занят анимацией появления и уходом после согласия. Она сознательно низкая: текст и кнопка всегда в одной строке (`flex-wrap: nowrap` — уехавшая под текст кнопка удваивала высоту на телефоне), а у кнопки снят `min-height: 52px` от `.btn` (осталось 40px — комфортная цель для пальца). От 1040px текст умещается в одну строку: `max-width` там до 780px, а `.cookie-text` занимает ровно себя. Ширину сверху режет зона кнопки связи — её габарит вычитается с обеих сторон (несимметричный отступ сбил бы центрирование). Угол справа внизу занят виджетом связи и кнопками прокрутки, поэтому до 760px плашка встаёт над ними (`--fab-size`, как панель чата), а дальше опускается к низу — по бокам её держат auto-поля, и до кнопки связи остаётся зазор. `z-index: 44` — ниже кнопки связи (45) и панели чата (46): диалог с гидом важнее уведомления.
- Текст сознательно лапидарный — «Мы используем файлы **Cookies** для аналитики и правильной работы сайта» (владелец, 26.08.2026: без перечисления, какие именно cookie и зачем). Ссылка на `/privacy` висит на самом слове «Cookies», отдельного «Подробнее» нет, поэтому фраза собирается из кусков `CookieTextBefore`/`CookieLink`/`CookieTextAfter` — место слова в предложении у трёх языков разное (тот же приём, что у согласия в форме и `.final-cta-lead`); пробелы вокруг ссылки живут внутри значений ключей. **Аналитика в тексте — аванс**: счётчиков на сайте пока нет, свои cookie (язык, тема, диалог чата) — технические, поэтому кнопок «принять/отклонить» нет. Как только появится реальная метрика, плашке понадобится настоящий выбор с хранением решения, а `/privacy` придётся дополнить. **Появится метрика — плашке понадобится настоящий выбор и его хранение**, а `script-src` в CSP придётся расширять точечно.

### Виджет связи

Плавающая круглая кнопка в правом нижнем углу ([ContactWidget.razor](src/frontend/ui.public.web/Components/ContactWidget.razor), подключается в `MainLayout`): фото гида в аврора-кольце, по клику вверх разворачивается «ёлочка» каналов — звонок (`CONTACT_PHONE`), Telegram-бот (`TG_BOT_URL`), MAX (`MAX_URL`) и чат на сайте (всегда, см. «Чат с посетителем»). Пузырь `.contact-teaser` («Остались вопросы?») объясняет, что фото — это путь к диалогу: одно лицо так не читается.

- **Меню — нативный `<details>` без JavaScript**, как `.nav-burger` и маркеры карты: закрытие кликом вне — фиксированный оверлей в `.contact-fab::before`. Отсюда та же ловушка: на `.contact-widget` и `.contact-fab` **нельзя** вешать `transform`/`filter`/`backdrop-filter` — они создают containing block и ломают оверлей. Панель обязана иметь `z-index: 1` (оверлей — `0`), иначе пункты видно, но нажать нельзя.
- **Раскрытие анимируется `animation`, а не `transition`**: `details` показывает содержимое скачком из `display: none`, переходу не от чего стартовать. Задержки «ёлочки» считаются `:nth-last-child` — новый канал дописывается в список без правки CSS.
- **Угол принадлежит виджету**: `.scrollers` и панель чата подняты над ним через токен `--fab-size` (`bottom: calc(16px + var(--fab-size) + 12px)`). Сам размер кнопки — 56px, на десктопе (≥900px) 72px: в углу она единственный «живой» элемент, и мелкий кругляш там терялся (владелец, 25.08.2026). Меняешь размер — правишь только токен, соседи подвинутся сами. Липкая полоса заявки внизу (`.mobile-apply` + `sticky-cta.js`) **удалена 21.08.2026** — два фиксированных элемента внизу мешали друг другу; её роль на узких экранах играет кнопка `.header-apply` в шапке.
- Каналов нет — виджета нет вовсе; ровно один — кнопка ведёт прямо в него, без меню.
- Иконка MAX — нейтральный глиф, а не фирменный знак: логотип это торговая марка, а CDN запрещён CSP. Появится официальный SVG — класть в `wwwroot/img/`.
- [contact-widget.js](src/frontend/ui.public.web/wwwroot/js/contact-widget.js) добавляет только то, чего `<details>` не умеет: Esc и сворачивание после выбора канала. Без него виджет полностью рабочий.
- Следующий шаг — пункт «Чат на сайте» (ключ `ContactChat` уже заведён в трёх resx): серверная часть чата готова, не хватает UI (см. ниже).

### Чат с посетителем

Плавающий чат на сайте: посетитель пишет вопрос → сообщение в БД chat-сервиса → пуш в приватную Telegram-группу гидов → гид отвечает **reply** прямо в группе → ответ появляется в чате на сайте. Сделан **отдельным сервисом** 21.08.2026: чат тянет за собой Telegram long polling, а он привязывает сервис к одному инстансу — держать на этом ещё и users было бы неоправданно.

Работает целиком: серверная часть (сервис `chat`, бот) плюс UI — панель [ChatPanel.razor](src/frontend/ui.public.web/Components/ChatPanel.razor), эндпоинты `/chat/send` и `/chat/poll` в UI и [chat.js](src/frontend/ui.public.web/wwwroot/js/chat.js).

- **UI чата** ([ChatPanel.razor](src/frontend/ui.public.web/Components/ChatPanel.razor)): панель открывается пунктом виджета связи. Без JavaScript пункт — обычная ссылка `?chat=open`: панель и историю рендерит сервер, отправка идёт POST'ом на `/chat/send` и возвращается сюда же (PRG). С JavaScript [chat.js](src/frontend/ui.public.web/wwwroot/js/chat.js) открывает её на месте и опрашивает `/chat/poll` — 3 с при открытой вкладке, 15 с в фоне, экспоненциальный откат до минуты при ошибках. Историю сервер тянет **только при `?chat=open`**, иначе каждый рендер каждой страницы ходил бы в chat-сервис.
- **Эндпоинты живут на сайте, а не на шлюзе** (`Program.cs`, до `MapRazorComponents`): в chat ходит сервер UI, он же владеет cookie. `POST /chat/send` сам проверяет antiforgery (`UseAntiforgery` покрывает только form-binding, а форму читаем вручную) и отвечает JSON при `Accept: application/json`, иначе редиректом — один код-путь и для JS, и без него. `GET /chat/poll` отдаёт `no-store` и `Vary: Cookie`.
- **Cookie `chat_sid`** ([ChatCookie](src/frontend/ui.public.web/Features/Chat/ChatCookie.cs)) — HttpOnly, Lax, 90 дней (совпадает с `CHAT_RETENTION_DAYS`). Значение приходит из сервиса в `ExecuteRequestResult.Value` и ставится в эндпоинте: из Razor-компонента cookie не поставить, как и у `/set-culture`.
- **Онлайн-статус** — [ChatSchedule](src/frontend/ui.public.web/Features/Chat/ChatSchedule.cs) по ключам `CHAT_HOURS` и `CHAT_TZ`: в часы работы «отвечаем за несколько минут», вне — «ответим утром». Кривой формат не прячет виджет, а пишет warning и считает, что чат отвечает всегда.
- **Лимитер UI** получил свои партиции для `/chat/poll` (40/мин) и `/chat/send` (10/мин): иначе поллинг съедал бы общий лимит страницы, а отправка — квоту формы заявки (5 за 5 минут).
- Тексты для скрипта едут `data-*` атрибутами (inline-скрипт запрещён CSP), сообщения рисуются только через `textContent` — `HtmlSanitizer` в проекте ни к чему не подключён, а текст приходит из чужой реплики.
- **Сущности** ([Chat.Domain/Entities/Chat.cs](src/backend/chat/modules/Chat.Domain/Entities/Chat.cs)): `ChatSessionEntity` (диалог) и `ChatMessageEntity`. Связь — плоским `SessionId`, без навигационных свойств и `Include`.
- **Идентификация диалога — только секретный `Token`** (32 байта CSPRNG в base64url), он же значение cookie `chat_sid` на сайте. Именно отдельный токен, а не `Id`: `Id` генерируется последовательным Guid'ом и предсказуем по соседям. Контакты, IP и User-Agent посетителя не хранятся сознательно.
- **Курсор поллинга — `Ordinal`** (1..N внутри диалога), не время: `Audit.CreatedAt` приходит из `NOW()`, и две вставки в одну микросекунду заставили бы фильтр «строго больше» пропустить сообщение. Защищён уникальным индексом `(SessionId, Ordinal)`.
- **Эндпоинты**: `POST /api/public/chat/send` (пустой или неизвестный токен заводит новый диалог — иначе первое сообщение отправить нельзя; в ответе `value` = токен сессии, `hash` = `Ordinal`) и `GET /api/public/chat/messages?token=…&after=N`. Без токена выборка пустая — чужую переписку получить нельзя. `ChatAdminReplyCommand` эндпоинта не имеет: её шлёт только бот изнутри процесса.
- **Антиспам в самом сервисе** (кроме лимитеров сайта и шлюза): ≤1000 символов, ≤20 сообщений на диалог за 10 минут и ≤100 всего → `ExcecuteCommandException(TooManyRequests)` → 429. Лимиты — в [ChatLimits](src/contracts/Chat.Contracts/ChatLimits.cs), общем для бэка и фронта.
- **Доставка в Telegram — через outbox, а не напрямую из обработчика**: `AfterCommit` кладёт id в `IChatNotificationQueue` (Channel в памяти), [ChatNotificationDispatcher](src/backend/chat/service/Bot/ChatNotificationDispatcher.cs) отправляет и проставляет `TgMessageId`. Держать POST посетителя на сетевом вызове Telegram нельзя, а падение мессенджера не должно ломать отправку. Очередь не переживает рестарт — **это нормально**: недоставленное видно в БД (`TgMessageId IS NULL AND Direction = 0`, под это есть частичный индекс), диспетчер раз в минуту подметает такие сообщения моложе 24 часов.
- **Reply → диалог**: сначала по `ChatMessages.TgMessageId`, потом по `ChatSessions.TopicMessageId` («шапка» диалога в группе). Уникальный индекс по `TgMessageId` плюс проверка в handler'е отсекают дубль при повторной доставке апдейта.
- Текст посетителя уходит в группу с `ParseMode.Html`, поэтому **обязательно экранируется** (`WebUtility.HtmlEncode`), иначе Telegram отвергнет сообщение или съест кусок текста.
- **Тексты сообщений не логируются нигде** — только `SessionId`, `Ordinal` и статус. `EnableSensitiveDataLogging` не включать.
- **Чистка по сроку хранения** ([ChatRetentionService](src/backend/chat/service/ChatRetentionService.cs), `CHAT_RETENTION_DAYS`, по умолчанию 90 дней) — **жёсткое** `ExecuteDelete`, вопреки общему для проекта мягкому удалению: в чате лежат ПД, `IsDeleted = true` оставил бы их в базе навсегда. Копию в Telegram это не убирает — Bot API не даёт удалять сообщения старше 48 часов, о чём должна честно говорить политика конфиденциальности.

### Telegram-бот

Хостится как `BackgroundService` в **chat-сервисе** ([chat/service/Bot/](src/backend/chat/service/Bot/), регистрация в `Configurator.ConfigureDependencies`), отдельного деплоймента нет. Long polling — исходящее соединение, поэтому закрытость сервиса от внешней сети боту не мешает.

**⚠️ chat-сервис нельзя масштабировать горизонтально**: long polling обязан работать ровно в одном экземпляре — два процесса с одним токеном начнут отбирать апдейты друг у друга, и ответы гидов будут теряться. Раньше от этого зависело только приветствие бота в users, теперь — вся входящая половина чата. Понадобится масштабирование — бота выносить в отдельный процесс.

- Клиент Telegram создаётся один на процесс в [TelegramBotAccessor](src/backend/chat/service/Bot/TelegramBotAccessor.cs); им пользуются и polling, и доставка сообщений.
- Без `TG_BOT_TOKEN` (или при отвергнутом Telegram'ом токене) бот пишет в лог и выключается, не мешая API. Без `TG_ADMIN_CHAT_ID` чат работает и сохраняет переписку, но в группу ничего не уходит — недоставленное уедет само, как только ключ появится.
- **`DropPendingUpdates = false`** (раньше было `true`): тем же каналом приходят ответы гидов, терять их нельзя. От пачки приветствий на вчерашние `/start` защищает фильтр по возрасту сообщения (10 минут). Telegram хранит недоставленные апдейты только 24 часа — за этим пределом ответ теряется независимо от нас.
- `AllowedUpdates = [Message, MyChatMember]`. `MyChatMember` нужен для настройки: добавили бота в группу — её id сразу виден в логе, его и класть в `TG_ADMIN_CHAT_ID`.
- **Privacy mode выключать не нужно**: в группе бот по умолчанию видит команды и reply на собственные сообщения — наш сценарий ровно такой. Обратная сторона: свободное (не reply) сообщение гида бот не увидит вовсе и подсказку на него не отправит.
- Язык ответа в личке — по `Message.From.LanguageCode`: `ru` → русский, `zh*` → китайский, остальное и пустой тег → английский. Язык служебных сообщений **в группе** — из `TG_ADMIN_LANG` (группа одноязычная). Тексты — switch в [BotTexts.cs](src/backend/chat/service/Bot/BotTexts.cs), UI-локализация (resx) тут не используется.
- Ссылка на бота в шапке сайта рендерится только при заданном `TG_BOT_URL` (в Development стоит плейсхолдер `https://t.me/kola_north_bot` — заменить на реальный username бота).

### Фото главной: хранилище и reverse proxy

Кадры полосы «Что вас ждёт» лежат **не в репозитории, а в хранилище** — владелец заливает их через веб-консоль MinIO, без коммита и пересборки сайта. Ради этого S3 и заведён; остальная статика (css, js, SVG-слои, `og.jpg`) остаётся в `wwwroot` и версионируется вместе с кодом — иначе появилось бы состояние «новая разметка + старый CSS».

- **Раскладка бакета** — один префикс `hero/`, имена файлов ровно те же, что в [HeroSlides](src/frontend/ui.public.web/Features/Home/HeroSlides.cs) (`hero-edge.webp` и т.д.). Имя обязано быть в нижнем регистре из `a-z0-9-` и заканчиваться на `.webp`: кириллица, пробелы и верхний регистр дают percent-encoding и «файл залит, а на сайте его нет». Непринятые файлы [MediaCatalog](src/frontend/ui.public.web/Features/Media/MediaCatalog.cs) пишет в лог поимённо — искать причину молча не придётся.
- **Как заливать**: `ssh -L 9001:127.0.0.1:9001 user@server` → `http://localhost:9001` → бакет `media` → папка `hero` → Upload. Консоль наружу не публикуется никогда, только через туннель.
- **`MediaCatalog`** держит снимок содержимого бакета в памяти, обновляет его `MediaRefresher` раз в `MEDIA_REFRESH_MINUTES` (он же — задержка «залил → видно на сайте»). Ходить в хранилище на рендере нельзя: главная отрисовывается на каждый запрос. Хранилище недоступно — живёт прошлый снимок, страница не падает.
- **Порядок источников в Home.razor**: объект в хранилище → одноимённый файл в `wwwroot/img` (так работает локальный `dotnet run` без докера) → `placeholder.svg`. Локальные копии кадров сознательно оставлены как фоллбек.
- **Версия в URL** (`?v=<время изменения объекта>`) проставляется автоматически: картинки кэшируются на 30 суток, и без неё перезалитое под тем же именем фото посетитель увидел бы только через месяц. nginx проксирует в MinIO **только путь**, поэтому лишний параметр до хранилища не доходит. Правило «новое фото — новое имя» из-за этого не нужно.
- **Пустой `MEDIA_ENDPOINT` выключает хранилище целиком** — сайт работает на локальных файлах. Та же идиома, что у `TG_BOT_URL` и `MAX_URL`.
- **Единственная копия фото — том `minio_volume`.** Он уже терялся при перестроении compose; оригиналы держать у себя локально, плюс регулярный `mc mirror --overwrite local/media <куда-то>`.

**nginx — единственная точка входа** ([infra/nginx/templates/default.conf.template](infra/nginx/templates/default.conf.template)): `/media/*` уходит в MinIO, всё остальное — в `ui-public`. Отсюда главное свойство: фото отдаются **с того же домена**, поэтому CSP (`img-src 'self' data:`) трогать не нужно и второй сертификат не нужен. Грабли, каждая из которых уже была наступлена:

- **`proxy_set_header X-Forwarded-*` обязателен.** nginx, в отличие от Caddy, не ставит эти заголовки сам, а `USE_FORWARDED_HEADERS` у UI включён. Забудешь — Kestrel увидит адрес контейнера nginx, один на всех, и rate limiter станет общим счётчиком: шестая заявка за 5 минут словит 429 у всех подряд.
- **Конфиг — шаблон, его прогоняет envsubst** (штатный механизм образа). `NGINX_ENVSUBST_FILTER: "^(SITE_|MEDIA_)"` в compose **обязателен**: без фильтра envsubst подставит пустые значения в переменные самого nginx (`$uri`, `$binary_remote_addr`, `$proxy_add_x_forwarded_for`), и конфиг молча станет нерабочим.
- **`resolver 127.0.0.11` обязателен**, потому что в `proxy_pass` медиа есть переменная `$obj`: из-за неё nginx резолвит имя upstream'а на каждом запросе, а не на старте, и без резолвера отвечает 502 «no resolver defined to resolve minio».
- **Защита маршрута — сама регулярка** `~* ^/media/(?<obj>.+\.webp)$`: перечисление бакета (`?list-type=2`), `.svg`, `.html` и подписанные ссылки в этот `location` просто не попадают и получают 404. Плюс `limit_except GET HEAD`. Порт 9000 (S3 API) не публикуется вовсе.
- **`?` в конце `proxy_pass`** отбрасывает query — до MinIO она не доходит.
- **`Content-Type` переопределяется только парой `proxy_hide_header` + `add_header`**: `add_header` сам по себе не заменяет заголовок, а MinIO при заливке некоторыми клиентами ставит `application/octet-stream`, и браузер тогда предлагает скачать файл вместо показа. По той же причине прячется чужой `X-Content-Type-Options` — иначе он придёт дважды.
- **`add_header` без `always`** не попадает в ответы 4xx/5xx; **`add_header` внутри `location` отменяет унаследованные** из `server` — перечислять все нужные в каждом блоке.
- **`Cache-Control` 30 суток, но без `immutable`**: версия в query и так решает вопрос, а `immutable` спрятал бы правку даже по Ctrl+F5.
- **`server_tokens off`** убирает версию, но полностью снять заголовок `Server` стоковый nginx не умеет (нужен модуль `headers_more`, которого в официальном образе нет) — снаружи видно `Server: nginx`.
- **`limit_req` на `/media`** (30 r/s, burst 60): маршрут публичный, фото весят сотни килобайт, без лимита их можно выкачивать в цикле. Это и было причиной выбрать nginx вместо Caddy.
- **TLS — в прод-оверлее, а не в этом конфиге.** Локальный шаблон остаётся http-only: боевой сайт поднимается вторым файлом `docker-compose.prod.yml`, который подменяет каталог шаблонов на [templates.tls/](infra/nginx/templates.tls/) (редирект с :80 на канонический хост плюс 443-блок с обоими `location`, продублированными дословно — `add_header` внутри `location` отменяет унаследованные из `server`) и монтирует `${CERT_DIR}` в `/etc/nginx/certs`. Сертификат **коммерческий, reg.ru** (не Let's Encrypt): certbot не используется, продление раз в год руками, приватный ключ reg.ru отдаёт только в момент выпуска и повторно не показывает. Имена файлов внутри каталога фиксированы (`fullchain.pem`/`privkey.pem`) и **не содержат домена**: `${SITE_DOMAIN}` может нести два имени через пробел, и путь с пробелом внутри не дал бы nginx стартовать. Канонический адрес — отдельный ключ `SITE_HOST` (одно имя, на него ведут редиректы с http и с www); фильтр `NGINX_ENVSUBST_FILTER` его пропускает, менять фильтр не нужно. **HSTS вписывать только в 443-блок**: на `http://localhost` он заставил бы браузер запомнить localhost как https-only и сломал бы прочие локальные проекты; начат с `max-age=300`, поднимать до года после нескольких дней стабильной работы. `location /.well-known/acme-challenge/` и тома `certbot_www`/`letsencrypt` оставлены на случай переезда на Let's Encrypt. Порядок развёртывания целиком — [docs/deploy.md](docs/deploy.md).

### Данные

Сущности разложены по сервисам. В users — `Application` (заявки, то, ради чего сайт) и `User` (эндпоинты `/api/admin/user/*` есть, но потребителей у них пока нет). В chat — `ChatSession` и `ChatMessage` (см. «Чат с посетителем»).

- Раздельные контексты чтения и записи в каждом сервисе: `ReadApplicationDbContext`/`WriteApplicationDbContext` в users и `ReadChatDbContext`/`WriteChatDbContext` в chat (read-контексты глобально NoTracking), строки подключения `DB_READ_CONNECTION_STRING` / `DB_WRITE_CONNECTION_STRING` у каждого свои.
- Схема выбирается через `search path` в строке подключения (users → `search path=users,public`, chat → `search path=chat,public`) — одна база, разные схемы; переезд на отдельные БД делается правкой строки подключения, без изменений в коде. EF генерирует SQL без указания схемы. `WriteDbContext.MigrateAsync` перед миграцией создаёт схему, расширения `citext` / `pg_trgm` / `btree_gin` и коллацию `case_insensitive` — поэтому новую схему заводить руками не нужно, достаточно строки подключения.
- Базовые контексты зарегистрированы factory-алиасами на конкретные (`AddScoped<ReadDbContext>(sp => sp.GetRequiredService<TR>())`), иначе `UnitOfWork` и репозиторий получили бы разные инстансы. Подробности — в комментарии внутри `AddPersistence`.
- Оптимистичная блокировка: `Entity.Xmin` замаплен на системный столбец `xmin` как concurrency token; update-команды несут `Xmin`, репозиторий кладёт его в `OriginalValue`.
- Удаление мягкое: `IsDeleted = true`, list-запросы фильтруют по нему (`GetBaseDbQuery`). Исключение — чистка переписки чата по сроку хранения: там жёсткое `ExecuteDelete`, потому что мягкое оставило бы персональные данные в базе навсегда.
- `SaveChanges` в handler'ах не вызывается: `CommandHandler.Handle` делает `UnitOfWork.CommitAsync` после `ExecuteCommand`; для пост-коммит эффектов — `AfterCommit`.
- `EntityConfig` по умолчанию навешивает **уникальный** индекс на `Title` — где заголовки повторяются, переопределяй `ConfigureIndexes` (так сделано в `ApplicationConfig`: два тёзки должны иметь возможность записаться). `UserEntity.Title` — stored computed column из `FirstName`/`LastName`.

### Безопасность

Что уже настроено и почему именно так:

- **Rate limiting в двух местах.** Форма отправляется POST'ом на сам сайт, а API-вызов делает сервер UI, поэтому ограничение только на шлюзе обходится отправкой формы в цикле. В UI глобальный лимитер делит запросы по методу: POST — 5 за 5 минут на IP, остальное — 300 в минуту. На шлюзе маршрут `applications-public` помечен политикой `public-form` (те же 5 за 5 минут), `chat-public` — политикой `public-chat` (300/мин), плюс глобальные 300/мин; `/health` из лимитов исключён. Счётчики живут в памяти процесса — при нескольких инстансах фактический лимит умножается на их число.
- **Лимиты шлюза для чата — не «на посетителя».** Шлюз видит IP сервера UI (страницы рендерит он, а не браузер), поэтому `public-chat` — общий потолок пропускной способности чата. Справедливость между посетителями обеспечивает только лимитер сайта, и когда на UI появятся эндпоинты чата, ему понадобятся отдельные партиции по пути (глобальный лимитер UI — именно `GlobalLimiter`, `.RequireRateLimiting(...)` его не заменит, а `.DisableRateLimiting()` снимет вообще всё).
- **CORS сознательно узкий и, по сути, задел на будущее.** Браузер в API не обращается (страницы рендерит сервер UI), поэтому политика собирается строго из `UI_APP_URL`, без `AllowCredentials`, и не регистрируется вовсе, если список пуст. Прежняя dev-ветка с `SetIsOriginAllowed(_ => true)` + `AllowCredentials()` убрана: такое сочетание разрешало любому сайту читать ответы API с cookie пользователя.
- **Заголовки ответа UI**: CSP (`script-src 'self'` — только собственные файлы скриптов, inline-скрипты запрещены; `style-src 'self'` — никаких inline-стилей в **нашем** коде), `X-Content-Type-Options`, `X-Frame-Options: DENY`, `frame-ancestors 'none'`, `Referrer-Policy: no-referrer`, `Permissions-Policy`. Заголовок `Server` отключён у обоих сервисов. Для сторонних скриптов (капча, метрика) их домены добавляются в `script-src` точечно. **При включённой капче CSP расширяется** (условно, в Program.cs): хост виджета в `script-src`/`frame-src`/`connect-src`, `font-src yastatic.net` (шрифты бейджа) и — вынужденно — `'unsafe-inline'` в `style-src`: виджет ставит инлайновые style-атрибуты в родительскую страницу (позиция бейджа и попапа с пазлом), под `style-src 'self'` их резало и пазл было не показать, а хеши меняются с каждым обновлением виджета (проверено 28.08.2026). Скрипты остаются под замком в обоих режимах.
- **Antiforgery** — включён (`UseAntiforgery`), токен в форму добавляет `EditForm`.
- **Валидация телефона** по формату, а не только по длине: поле уходит оператору и в бота, без формата форма становится каналом для спам-ссылок. Имя необязательно и ограничено длиной.
- **Лимит тела запроса** в UI — 64 КБ (загрузок файлов нет). Появится загрузка — поднять.
- **`USE_FORWARDED_HEADERS`** у UI в compose включён (`true`) — сайт стоит за nginx. Флаг и отсутствие публичного порта у `ui-public` **связаны намертво**: код делает `KnownProxies.Clear()`, то есть доверяет `X-Forwarded-For` от кого угодно, и стоит открыть порт наружу — любой обойдёт rate limiting одним заголовком. Без флага было бы не лучше: Kestrel видел бы адрес контейнера nginx, один на всех, и шестая заявка за 5 минут ловила бы 429 у всех подряд. Проверяется так: исчерпать лимит `/chat/poll` (40/мин) с одного адреса и тут же сходить с другого — второй должен получить 200.
- **users и chat не должны торчать в интернет** — они никого не проверяют и доверяют вызывающему; в `docker-compose.yml` их порты (5012 и 5013) открыты только для локальной отладки.

Уведомление о cookie есть (26.08.2026, см. «Уведомление о cookie»). Согласие на обработку ПД и политика конфиденциальности **есть, но как заглушки** (добавлены 18.08.2026): чекбокс `.consent` в форме проверяется только в браузере (`required` + form-ui.js), в контракт не входит и сервером не валидируется; страница `/privacy` — честный черновик (реквизиты и почта уже настоящие; раздел «Отзывы и фотографии гостей» — про согласие на публикацию и его отзыв — добавлен 28.08.2026 вместе с блоком отзывов, ключи `PrivacyS5*`; почта рендерится из `CONTACT_EMAIL` рядом с реквизитами, не из resx). Перед запуском: настоящий текст политики, при желании — серверная проверка согласия (поле в контракте + валидатор).

- **Капча**: невидимая Яндекс SmartCaptcha на форме заявки (28.08.2026), включается **парой** ключей `SMARTCAPTCHA_CLIENT_KEY`/`SMARTCAPTCHA_SERVER_KEY`; половинная конфигурация = выключено + warning в логе ([SmartCaptchaService](src/frontend/ui.public.web/Features/Captcha/SmartCaptchaService.cs)). Клиентский ключ **настоящий** (владелец создал капчу 28.08.2026, ключ публичный — лежит дефолтом в appsettings.Development.json и docker-compose); серверный — секрет, **только в `.env`**, и пока владелец его не вписал — капча выключена целиком: ни виджета, ни проверки, ни каптча-хостов в CSP. Схема: [smart-captcha.js](src/frontend/ui.public.web/wwwroot/js/smart-captcha.js) перехватывает submit **на document (bubble)** — гарантированно после валидации form-ui.js, которая висит на самой форме, чтобы не жечь одноразовые токены (живут 5 минут) на невалидной форме, — вызывает `execute()`, токен из callback кладёт в hidden `smart-token` и делает `requestSubmit()` (полный цикл события — phone-intl.js должен собрать номер с кодом). Сервер проверяет токен в `SubmitAsync` POST'ом на `smartcaptcha.cloud.yandex.ru/validate` (хост валидации ≠ хост виджета `smartcaptcha.yandexcloud.net` — так в доках). **Отсутствие токена — отказ** (`CaptchaFailed` в resx): с включённой капчей заявка требует работающий JavaScript (правило «всё без JS» снято владельцем в тот же день — см. раздел UI). **Недоступность сервиса капчи — fail-open** (пропуск с warning'ом; так рекомендуют и сами доки — не-200 от validate считать «ok»): сбой Яндекса не должен терять заявки. CSP расширяется условно в Program.cs (script-src/frame-src/connect-src + хост виджета, только при включённой капче). Бейдж «обработка данных» **скрыт** (`hideShield: true` — в Safari его крестик не закрывал плашку, а состояние не запоминалось; владелец, 28.08.2026): условия сервиса разрешают это только вместе с собственным уведомлением — оно стоит под кнопками формы (`.captcha-note`, ключи `CaptchaNote*`; URL зависит от языка: ru → yandex.ru/legal/smartcaptcha_notice, en/zh → yandex.com). Для отладки у `render` есть параметр `test: true` (задание показывается всегда) — в код не вшит, включается временно руками.

Чего ещё нет и о чём стоит помнить: бэкапов БД и хранилища фото, TLS-сертификата (реверс-прокси уже есть — nginx; включение TLS описано в разделе про него). `EnableSensitiveDataLogging` в EF не включён — включать нельзя, иначе телефоны попадут в логи.

### Остатки, которые ещё ни к чему не подключены

- В `UI.Shared` из handler'ов используется только `ApiCommandHandler`; `ApiSingleQueryHandler`, `ApiListQueryHandler`, `ApiPagedListQueryHandler`, `QueryBuilder`, `ConfigService` и `AuthorizationHeaderHandler` (с ключом `API_TOKEN`) остались от страницы заявок и сейчас без потребителей.
- `SanitizationBehavior` + `HtmlSanitizer` написаны, но ни один сервис не добавляет behavior в `PipelineBehaviors`.
- Серверная локализация (`SharedStrings.resx` в Api, `UsersStrings.resx` в users, `SharedLocalizer`) заведена, но не используется — рабочая локализация есть только в UI (`SharedResource`).
- Абстракции `ISlugable` / `IPictureable` и поле `Dto.ImagePath` есть в базовых типах, но ни одна сущность их не реализует; файлового сервиса и хранилища в проекте нет.

## Конфигурация

Настройки — плоские ключи в стиле переменных окружения (не иерархические секции), читаются напрямую из `IConfiguration`:

| Сервис | Ключи |
|---|---|
| UI | `API_URL`, `API_TOKEN`, `CONTACT_PHONE` (пункт «Позвонить» в виджете; пусто → пункта нет), `CONTACT_EMAIL` (строка в футере и почта на `/privacy`; пусто → строки нет), `SMARTCAPTCHA_CLIENT_KEY` / `SMARTCAPTCHA_SERVER_KEY` (невидимая капча формы заявки; пусто — выключена, см. «Капча» в Безопасности), `CHAT_HOURS` / `CHAT_TZ` (часы работы чата; по умолчанию 09:00-21:00 и Europe/Moscow), `TG_BOT_URL` (ссылка в шапке и пункт виджета связи; пусто → нет ни того, ни другого), `MAX_URL` (пункт MAX в виджете связи; пусто → пункта нет), `SITE_URL` (база canonical/OG/sitemap; пусто → абсолютные SEO-ссылки отключены), `USE_FORWARDED_HEADERS`, `MEDIA_ENDPOINT` (адрес S3; пусто → фото берутся из wwwroot), `MEDIA_BUCKET`, `MEDIA_PUBLIC_PATH` (путь, по которому nginx отдаёт бакет), `MEDIA_ACCESS_KEY` / `MEDIA_SECRET_KEY` (учётка только на чтение), `MEDIA_REFRESH_MINUTES` |
| gateway | `JWT_ISSUER`, `JWT_KEY`, `UI_APP_URL` (белый список CORS), `USE_FORWARDED_HEADERS` |
| users | `DB_READ_CONNECTION_STRING`, `DB_WRITE_CONNECTION_STRING`, `HTTP_PORT` |
| chat | `DB_READ_CONNECTION_STRING`, `DB_WRITE_CONNECTION_STRING` (схема `chat`), `HTTP_PORT`, `TG_BOT_TOKEN` (пусто → бот выключен), `TG_ADMIN_CHAT_ID` (группа гидов; пусто → сообщения сохраняются, но не уходят), `TG_ADMIN_LANG` (язык служебных сообщений в группе, по умолчанию `ru`), `CHAT_RETENTION_DAYS` (срок хранения переписки, по умолчанию 90; `0` и меньше выключают чистку), `SITE_URL` (куда ведёт кнопка бота) |
| minio | `MINIO_ROOT_USER`, `MINIO_ROOT_PASSWORD` (обязательны — без них compose не стартует) |
| nginx | `SITE_DOMAIN` (`server_name`; несколько имён — **через пробел**), `SITE_HOST` (канонический адрес, одно имя; цель редиректов с http и с www — только в прод-оверлее), `MEDIA_BUCKET`, `MEDIA_MAX_AGE`, `NGINX_ENVSUBST_FILTER` (не менять) |
| все | `OTLP_EXPORT_ENABLED`, `OTLP_RECEIVER_ENDPOINT_HTTP`, `OTLP_RECEIVER_ENDPOINT_GRPC`, `LOGGING_MIN_LEVEL` |
| только прод ([docker-compose.prod.yml](docker-compose.prod.yml)) | `JWT_KEY` (у шлюза его нет в `appsettings.json` — в `Production` без него сервис падает на старте), `POSTGRES_PASSWORD` (подставляется в строки подключения, поэтому только буквы и цифры: `=` и `;` их ломают; на уже созданном томе не применяется), `SITE_HOST`, `CERT_DIR` (каталог с `fullchain.pem`/`privkey.pem` вне репозитория) |

Пароли хранилища живут в `.env` (он в `.gitignore`), шаблон — `.env.example`. Dev-значение `JWT_KEY` лежит в репозитории для локального запуска — в реальном развёртывании задаётся через окружение.

Сборка: `Directory.Build.props` задаёт `net10.0` / `LangVersion 14` / nullable / implicit usings; `src/backend/Directory.Build.props` добавляет `Mediator.SourceGenerator` и `EFCore.Design` во все Web-SDK проекты бэкенда. Версии пакетов — только в `Directory.Packages.props`, там намеренно нет неиспользуемых зависимостей.
